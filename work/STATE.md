# 現在地（2026-09-13）

旧プロジェクトの現行ソースとgohan.txtを取り込み、gohan専用リポジトリへ分離。現在の実装はチャット漢字候補のPC検証済み版。統合UIの実機確認は未実施。描画・しずえの正常復元基準はF394。

現在の依頼は配置とソースの独立push。今回はコード挙動の変更、プラグインの再ビルド、実機操作なし。次の開発は利用者の最新依頼と仕様に従う。詳細はSESSION.md。

最新訂正: 実機未確認は必ず作業用ブランチへpush。統合版はwork/chat-kanjiで継続し、mainは実機正常確認済みF394。ソースバックアップはGitで復元し、固有成果物だけ解析環境artifactsへ保持。今回ビルド・実機操作なし。

# gohan 仕様書

とびだせ どうぶつの森・日本語版無印（＋更新版）向け CTRPF プラグイン `gohan` の仕様。

> 項目の振る舞い（ON/OFF の意味、ホットキー、関数側との連携）は [gohan-menu.md](gohan-menu.md)。
> 記載された全機能が実装済みという意味ではない（[R2](#0-全体の規則)）。
> **未定** と書いた箇所は仕様が決まっていない。**別途** と書いた箇所は別の仕様書で詰める。

---

## 0. 全体の規則

| # | 規則 |
|---|---|
| **R1** | **各フォルダの中では、サブフォルダを項目より先に並べる。** `root` は木の頂点を指す呼び名で、画面上に「root」というフォルダは出さない |
| **R2** | **各チートは一つ一つ解析してから実装する。未解析のチートは実装しない。** この仕様書に載っていることと実装済みであることは別 |
| **R3** | **移植は完全なコピーではない。** 他プラグイン由来の機能も、拡張性を高くし最適な設計で実装し直す。`由来` 欄は着想と参照元を示すだけで、移植元の構造をそのまま持ち込む意味ではない |

### 項目の種別

| 種別 | 意味 |
|---|---|
| トグル式 | ON / OFF を切り替える |
| アクション式 | 選ぶと 1 回実行する |
| トグル型アクション式 | チートメニューから ON / OFF でき、**ON にすると必ず適用待ち状態になる。適用されると OFF に戻り、アクションが実行される**（→ [§15](#15-別途詰める)） |
| 数値入力 | 数値を入力する。`float` / `整数` / `16進数` の別がある |
| リストボックス式 | 選択肢から選ぶ |
| 連動型 | **メニューを開いたその時にゲーム側の現在値を 1 回読み取る。** 開いている間ずっと同期し続けるわけではない（同期し続けるとチートによる変更ができなくなる）。次に読み取るのはメニューを開き直した時。数値入力とリストボックス式に付く |

### ON / OFF とホットキーの関係

- **項目の ON / OFF はチートメニューから行う。**
- **トグル型アクション式は、ホットキー付きでもメニューのON→適用で即実行し、OFFへ戻る。** ホットキーでは確認ダイアログを表示し、承諾後に実行する。アクション式との違いはメニュー実行で適用待ちを経由することと、ホットキー実行で確認ダイアログを出すことのみ。持続的なON/アームは発動条件にしない。ホットキー設定変更自体は適用待ちを経由する（[gohan-menu.md §7.4](gohan-menu.md#74-ib_apply_once-とは)）。
- **ホットキーは、チートメニュー項目の ON / OFF 操作を保障するものではない。**
  **項目の ON/OFF** と **関数内の ON/OFF** は別物として扱う。詳しくは [gohan-menu.md](gohan-menu.md) §1。

### 表記

- 見出し `## root/プレイヤー/資産` は**メニューの木のパス**。`座標移動` とその子設定（移動キー・移動量・移動方法・十字キーの無効化）は `root/プレイヤー/座標移動` フォルダに入れる（2026-09-17 利用者指示）。フォルダ名はすべて日本語（2026-09-17 利用者指示）。
- 項目名の `->`（例: `座標移動 -> 移動キー`）は、**親項目にぶら下がる子設定**。
- ホットキーは `DPad Up` 形式で統一する（`↑` 等の別表記は使わない）。
- 由来の `Vapecord` のソースは解析環境の
  `reference/old_project/REFERENCES/CTRPF PROJECTS/Vapecord-ACNL-Plugin-master/`。

---

## 0.5 PatchList 表について

各項目の「PatchList（JPN）」表は、**PatchList（複数番地への貼る／剥がす）だけで実現できそうなチート**の番地と値の候補である（2026-09-17）。

- 番地はすべて **JPN 無印＋更新版**。USA 番地の資料は Vapecord の `Addresses.hpp`（JPN 列）で変換した。
- OFF 値は資料の値ではなく、**`artifacts/update/exefs/code.bin` の元の命令を読んだもの**。資料が OFF 値を書いている場合は一致を確認した。
- 照合と逆アセンブルは `tools/patches/patchlist_candidates.py`（結果 `work/evidence/patchlist/candidates.json`）、候補の比較は `tools/patches/patchlist_context.py`。
- **効果（ゲーム内でどう効くか）は解析していない。** R2 により、実装の前に各チートを解析する。
- `アイテムが消えない`・`キーボード制限解除` は**未解析**（実装方式も解析してから決める）。PatchList 以外のチートの設計は [§17](#17-patchlist-以外のチートの設計)。

## 1. メニューの木

R1 に従い、各階層はサブフォルダを先に置く。

```
root
├─ プレイヤー
│   ├─ 資産
│   ├─ スタイル
│   └─ 座標移動
├─ アイテム
│   └─ ドロップ
│       └─ お遊び
├─ 村
│   └─ マップ
├─ ゲーム
│   ├─ キーボード
│   └─ 店
├─ テスト
└─ チャットコマンド
```

---

## 2. root

### チャットコマンド

| 欄 | 内容 |
|---|---|
| 種別 | トグル式 |
| 既定ホットキー | `R+B` |
| 説明 | チャットにコマンドを打つ事で様々なチートを素早く実行できます。コマンドリストは list で確認できます。 |

コマンドの一覧は [§14](#14-チャットコマンド)。

---

## 3. root/プレイヤー

### 座標移動

> 設計: [§17.1](#171-座標移動押下中実行型)

| 欄 | 内容 |
|---|---|
| 種別 | トグル式 |
| 既定ホットキー | `A` |
| 説明 | 移動キーで高速移動できます。 |
| 由来 | AnimalBytes |

挙動:

- ホットキーを押しながら移動キーで指定されたキーを押すと、その方向へ移動する。
- スライドパッドと C スティックの場合は、可能なら 8 方向ではなくスライドパッドの向きに合わせて移動し、傾き度合いに合わせて移動量を調節できるようにする。

#### 座標移動 -> 移動キー

| 欄 | 内容 |
|---|---|
| 種別 | リストボックス式 |
| 選択肢 | 十字キー / スライドパッド / C スティック |
| 既定値 | 十字キー |
| 説明 | 座標移動に使うキーを指定できます。 |

#### 座標移動 -> 移動量

| 欄 | 内容 |
|---|---|
| 種別 | 数値入力 `float` |
| 既定値 | `5.0`（2026-09-24 利用者指示） |
| 範囲 | `0.1`〜`99.9`。十字キー（左右）で `0.5` ずつ変わる（2026-09-17 利用者指示） |
| 説明 | 座標移動の移動量を設定できます。 |

- **`移動方法` がグリッド単位のときは無効状態**にする（カーソルは置けるが編集できない。2026-09-18 利用者指示）。判定は `移動方法` の編集中の値で行い、切り替えるとすぐ反映する。グリッド単位は 1 マスずつ動くので移動量を使わない。

#### 座標移動 -> 移動方法

| 欄 | 内容 |
|---|---|
| 種別 | リストボックス式 |
| 選択肢 | 通常 / グリッド単位 |
| 既定値 | 通常 |
| 説明 | 座標移動の移動方法を変更できます。グリッド単位の時、スライドパッドによる向きは8方向に限定されます。 |

#### 座標移動 -> 十字キーの無効化

| 欄 | 内容 |
|---|---|
| 種別 | リストボックス式 |
| 選択肢 | なし / 座標移動中 |
| 既定値 | なし |
| 説明 | 座標移動中にすると、移動キーが十字キーの時、ホットキーを押している間はゲームの十字キーを無効にします。 |

挙動（2026-09-17 利用者指示）:

- `座標移動中` のとき、**移動キーが十字キーの場合に限り**、座標移動（項目 ON）のホットキーを押している間はゲーム内の十字キーの判定を無効にする。移動キーがスライドパッド / C スティックのときは何もしない。
- 実装は自前メニューの入力遮断ケーブ（`0x00838020`、`sead::Controller::calc` の `0x0053CDF4`）を流用する。制御ブロック `0x009B7010` +0 を「0 なし / 1 既知のボタン全部 / 2 十字キーだけ」にし、2 のとき hold から十字キー 4 ビット（`0x000F0000`）だけを落とす。
- プラグインのスレッドが毎フレーム `GuiMenu::BlockGameDpad()` を呼んでいる間だけ 2 を書く。ホットキーを離す・項目 OFF・メニュー表示中は次のフレームで戻る（メニュー表示中は元から全ボタン遮断）。
- 座標移動そのものは CTRPF 側の入力で読むので、ゲーム側の十字キーを止めても動く。

### タッチワープ

> 設計: [§17.2](#172-タッチワープトグル式毎フレーム)

| 欄 | 内容 |
|---|---|
| 種別 | トグル式 |
| 説明 | 地図をタッチした場所にワープします。 |
| 由来 | Vapecord |

挙動:

- **ホットキーを設定している場合は、そのホットキーを押している間だけ**タッチでワープする。未設定なら項目 ON の間いつでもワープする（2026-09-18 利用者指示）。

### 壁抜け

| 欄 | 内容 |
|---|---|
| 種別 | トグル式 |
| 既定ホットキー | `L+DPad Up` |
| 説明 | どこでも歩けるようになります。あなたの歩みを阻むものは何一つとして存在しません。 |
| 由来 | Vapecord |

**PatchList（JPN・3 語）** — 出典: 旧記載 `[壁抜け 3patches]`。**利用者が実機で動作確認済み**なので採用。

| 番地 | ON | OFF（元の命令） | ON の命令 | OFF の命令 |
|---|---|---|---|---|
| `0x0064F03C` | `0xE3A07000` | `0xE3A07001` | `mov r7, #0` | `mov r7, #1` |
| `0x0064F19C` | `0xE1A00000` | `0xED841A05` | `nop` | `vstr s2, [r4, #20]` |
| `0x0064F1B4` | `0xE1A00000` | `0xED840A07` | `nop` | `vstr s0, [r4, #28]` |

> 別候補: Vapecord `walkOver`（USA→JPN 変換）・JOKER / ROTATION / Adios（JPN 実装）の 8 語
> （`0x0064EEF4` `EA000094` / `0x0064EF0C` `EA000052` / `0x0064F070` `EA000001` / `0x0064F0E8` `EA000014` /
> `0x0064F19C` `E1A00000` / `0x0064F1B4` `E1A00000` / `0x0064F1B8` `EA000026` / `0x0064F1E4` `EA000065`）。後ろの 2 語の NOP は共通。

### アクション解除

> 設計: [§17.3](#173-アクション解除単発実行型)

| 欄 | 内容 |
|---|---|
| 種別 | トグル式 |
| 既定ホットキー | `L+DPad Down` |
| 説明 | 強制的に立ち状態に変更します。身動きが取れなくなった時用です。 |
| 由来 | Foxxy の Idle |

挙動:

- プレイヤーの動作状態を **Idle（state `0x06`）へ強制移行**させる。
  旧 F-66 で `player+0x1A9` が現在状態、状態 6 が neutral/wait であることを確認している。

### 気絶

> 設計: [§17.4](#174-気絶トグル型アクション式)

| 欄 | 内容 |
|---|---|
| 種別 | トグル型アクション式 |
| 説明 | 気絶します。身動きが取れなくなった時用です。 |

### 花散らせない

| 欄 | 内容 |
|---|---|
| 種別 | トグル式 |
| 説明 | もう友達の村の移動に気を遣う必要はなくなりました。花の上を走っても散らなくなります。 |
| 由来 | Vapecord |

**PatchList（JPN・1 語）** — 出典: AnimalBytes `DeceiveFlower`・ROTATION `NoBreakFlower`（JPN 実装、ON/OFF 明示）。花の状態を読む `BL 0x002FCCD4` を `MOV R0,#29` に置き換え、直後の `CMP R0,#29` → `BEQ` で処理を抜けるので採用。

| 番地 | ON | OFF（元の命令） | ON の命令 | OFF の命令 |
|---|---|---|---|---|
| `0x00596890` | `0xE3A0001D` | `0xEBF5990F` | `mov r0, #29` | `bl 0x2fccd4` |

> 別候補: Vapecord `unbreakableflower`（JPN `0x0059689C` `E3A0801D`）。同じ判定の `BEQ` を消して `R8=29` のまま処理を続ける形なので採用していない。

### 穴に落下しない

| 欄 | 内容 |
|---|---|
| 種別 | トグル式 |
| 説明 | 穴にハマらなくなります。残念でしたね。 |
| 由来 | Vapecord |

**PatchList（JPN・2 語）** — 出典: Vapecord / FOXXY `noTrap`（USA→JPN 変換）・`REFERENCES/CHEATS/player.txt`〈穴に落ちない〉（JPN、ON/OFF 明示）が同じ 2 語。独立した出典が一致しているので採用。

| 番地 | ON | OFF（元の命令） | ON の命令 | OFF の命令 |
|---|---|---|---|---|
| `0x00659160` | `0xEA000014` | `0x1A000014` | `b 0x6591b8` | `bne 0x6591b8` |
| `0x006774DC` | `0xEA00002D` | `0x1A00002D` | `b 0x677598` | `bne 0x677598` |

---

## 4. root/プレイヤー/資産

すべて **連動型数値入力・整数**。

| 項目 | 最小 | 最大 | 説明 | 由来 |
|---|---:|---:|---|---|
| 所持金 | 0 | 99999 | 所持金を変更します。 | Vapecord |
| 貯金 | 0 | 999999999 | ATMの貯金を変更します。 | Vapecord |
| ふるさとチケット | 0 | 9999 | ふるさとチケットを変更します。 | Vapecord |
| メダル | 0 | 9999 | オン島のメダルを変更します。 | Vapecord |

> `カブ` は店のカブ価でプレイヤーの資産ではなかったので、`カブ価` に改名して [§12.5](#125-rootゲーム店) へ移した（2026-09-17 利用者指示）。

**設計（2026-09-17 の静的な読み取り。実機未確認）**

どれもゲームの暗号化された値（8 バイト: u32 本体 / u16 / u8 シフト / u8 検査和）。読み書きはゲーム自身の関数を使う。

| 用途 | JPN 番地 | 根拠 |
|---|---|---|
| 復号 `DecryptValue(EncVal*) -> int` | `0x00303530` | Vapecord `MONEYGET`（JPN 列）。検査和が合わないと 0 を返す |
| 暗号化 `EncryptValue(EncVal*, int)` | `0x00303404` | Vapecord `MONEYSET`（JPN 列）。乱数でシフトを選ぶ |
| 自分のプレイヤーのセーブ `GetSaveOffset(4)` | `0x002FEB60` | Vapecord `PSOFFSET`。自分の番号なら `[0x00AA914C]`、ほかは `0x002FB920` のセーブ枠（全体の先頭 +0xA0 + 番号×0xA480） |
| セーブ全体の先頭 | `[0x00955F8C]`（`0x002FB354`） | Vapecord `D_GARDEN` |

| 項目 | 場所 | 独立した 2 経路 |
|---|---|---|
| 所持金 | プレイヤー +0x6F08 | ゲームが `sub_2FB900()+0x6F08` で復号（`0x0019C9AC` ほか）／Vapecord 注記 0x6FA8 − 0xA0 |
| 貯金 | プレイヤー +0x6B8C | ゲームが +0x6B8C を復号（`0x00114884` ほか）／Vapecord 0x6C2C − 0xA0 |
| メダル | プレイヤー +0x6B9C | ゲームが +0x6B9C を復号（`0x001ADA2C`）／Vapecord 0x6C3C − 0xA0 |
| ふるさとチケット | プレイヤー +0x8D1C | ゲームが +0x8D1C を復号（`0x001E4DEC` ほか）／Vapecord 0x8DBC − 0xA0 |

- 読み取り: メニューを開いた時に 1 回。検査和（`DecryptValue` と同じ式）が合わない、セーブが無い、範囲外なら読めない扱い（項目を編集できなくする。カーソルは置ける）。
- 書き込み: 適用で `EncryptValue`。
- 未確認: 項目と欄の対応（貯金とメダルは Vapecord の名前が根拠）、オンライン時の扱い、ゲームのスレッド外から暗号化関数（乱数を使う）を呼ぶ影響。

---

## 5. root/プレイヤー/スタイル

| 項目 | 種別 | 解析状況 | 説明 | 由来 |
|---|---|---|---|---|
| スタイルを変更 | アクション式 → 専用カスタムメニュー | 設定ごとに下記参照 | 未定（あとで追加する） | — |
| 寝癖付かない | トグル式 | — | 日にちを空けてログインしても寝癖が付かなくなります。 | Vapecord |

**R2 により、未解析の項目は解析を済ませるまで実装しない。**

### 寝癖付かない

**PatchList（JPN・1 語）** — 出典: Vapecord `NeverBedHead`（USA→JPN 変換）。候補はこれだけ（`STRB R1,[R4,#4]` を消す）。

| 番地 | ON | OFF（元の命令） | ON の命令 | OFF の命令 |
|---|---|---|---|---|
| `0x0020C6D8` | `0xE1A00000` | `0xE5C41004` | `nop` | `strb r1, [r4, #4]` |

> **どこで効くか（静的・2026-09-17、V2-F013）**: この 1 語は `Player_ApplyAbsenceBedHead`（`0x0020C688`）の中。選択中のプレイヤーの不在日数が 15 日以上のとき髪型を 16 / 33 にする書き込み。
> 呼ばれるのは日替わり処理で、**しずえ画面の「準備しています」の間にセーブ処理スレッドで走る SvProc 種別 4**（`SvProcType4_Prepare` `0x005CC724`）から届く。
> 経路と根拠は `work/evidence/prepare_screen/README.md`。

### スタイル変更の専用メニュー【確定・2026-09-14】

**スタイル編集の入口は`スタイルを変更`アクション一つとする。** 選択すると専用カスタムメニューを開き、そこで設定・実行する。ヘアスタイル等を独立した連動型チート項目として並べない。`寝癖付かない`は別のトグル機能として残す。

| 専用メニュー内の設定 | 解析状況 |
|---|---|
| ヘアスタイル | **実機確認済み**（F-68） |
| ヘアカラー | 静的のみ（F-66、MEDIUM） |
| 目の形 | **未解析** |
| 目の色 | 静的のみ（F-66、MEDIUM） |
| 性別 | **未解析** |

設定を編集した後、専用メニュー内の実行操作で指定したスタイルへ変更する。アクションを選んで専用メニューを開くことと、スタイル変更を実行することは区別する。

- **意図**: 文字だけでは、選んだときにどのような見た目になるか分かりづらい。
- **画像表示の方針**: 設定を選んでいる間、**横に見た目の画像を表示する**。
- 専用メニューの具体的な配置、設定・実行・取消の操作は GUI・メニュー設計の側で決める（→ [§15](#15-別途詰める)）。

### 解析済みの範囲

旧 `RE/FINDINGS.md` の F-66 が、見た目変更状態 `0xB9` のパケットとプロフィールの対応を出している。
**この表に載っているのは 3 つだけで、`目の形` と `性別` は含まれない。**

| 値 | プロフィール | パケット | 範囲 |
|---|---|---|---|
| 髪型（ヘアスタイル） | `+4` | `+14` | `0x00`-`0x21`（34 種） |
| 髪色（ヘアカラー） | `+5` | `+15` | `0x00`-`0x0F`（16 色） |
| 目の色 | `+7` | `+16` | `0x00`-`0x05`（6 色） |

- **`目の形` と `性別` は未解析。** プロフィール上の位置も取り得る値も未確定。
- **`ヘアカラー` と `目の色` は静的解析のみ**（F-66、confidence MEDIUM）。実機では確認していない。

### スタイルを変更

専用カスタムメニュー内の実行操作で、**そこで指定したスタイルに変更する**。

実装には **解析済みの「即時スタイル適用＋装備保持」経路**を使う。
根拠は旧 `RE/FINDINGS.md` の **F-68**（confidence HIGH。**オフライン実機で即時反映と衣装 8 枠の維持を確認済み**）。

要点:

- 状態 `0xB9`（通常の髪型変更）を経由しない。プロフィールの該当値を書いてから
  `vc_UPDATE`（`0x689DDC`）を呼ぶ。
- `vc_UPDATE` が同じプロフィールから髪色・衣装 8 枠を読み直すため、
  **帽子・アクセサリを保ったまま即時に反映される**。
- 状態 `0xB9` 内にしかない Hat / Accessory の消去（`0x6832E8`/`0x6832F4`、`0x6857A4`/`0x6857B0`）を通らない。

**F-68 が実機で確かめたのは「オフラインで `HairStyle`（プロフィール `+4`）を書く」ところまで。**
ほかの値へ同じ経路が使えるかは確認していない。オンラインの扱いは旧 F-66 / F-67 を参照。

---

## 6. root/アイテム

### ポケットアイテム

> 設計: [§17.5](#175-ポケットアイテムアクション式--16-進数入力)

| 欄 | 内容 |
|---|---|
| 種別 | アクション式 → 数値入力（16進数）の呼び出し |
| 説明 | 入力したIDのアイテムを持ち物の空いているスロットに入れます。 |

挙動:

- 入力されたアイテム ID を、持ち物の index 0 から**一番近い空いているスロット**に入れる。
- 持ち物を開いている時に入力された場合は、**持ち物画面の更新も行う**。

---

## 7. root/アイテム/ドロップ

### アイテムセレクター

| 欄 | 内容 |
|---|---|
| 種別 | トグル式 |
| 説明 | 未定（あとで追加する） |

挙動:

- 持ち物を開いている時、**ボタンを追加する**。
- そのボタンを押すと、寄贈する時などのようなアイテム選択ができるようになる。
- 選択したアイテム、または選択した位置のアイテムの ID を **`ドロップアイテム` に設定する**。

### ドロップアイテム

| 欄 | 内容 |
|---|---|
| 種別 | 数値入力式 |
| 説明 | ドロップ系チートが参照するアイテムIDを指定します。 |

**参照する「ドロップ系チート」は未定。** 今後追加するチートがこの値を参照する。

### 歩いた場所のアイテムを消し去る

| 欄 | 内容 |
|---|---|
| 種別 | トグル式 |
| 既定ホットキー | `R+DPad Up` |
| 説明 | あなたが歩いてきた場所に何かが残る事は決してありません。 |
| 由来 | Vapecord |

**PatchList（JPN・6 語）** — 出典: AnimalBytes `Trampler`〈歩いて消す〉（JPN 実装、ON/OFF 明示。元の命令と全一致）。
Trampler とも呼ばれる。FOXXY `trampleSeeder`（USA）も同じ系統の 6 語で、Vapecord の表で JPN に直せた 3 語（`0x00596870` / `0x0059689C` / `0x005968E4`）はこの表と一致する。
6 語とも `花散らせない` と同じ関数（`0x00596870` 付近）にあり、うち 5 語は抜け先 `0x005969D0` への条件分岐を消す。各分岐の意味は未解析。

| 番地 | ON | OFF（元の命令） | ON の命令 | OFF の命令 |
|---|---|---|---|---|
| `0x00596920` | `0xE1A00000` | `0x0A00002A` | `nop` | `beq 0x5969d0` |
| `0x00596870` | `0xE1A00000` | `0x0A000056` | `nop` | `beq 0x5969d0` |
| `0x0059689C` | `0xE1A00000` | `0x0A00004B` | `nop` | `beq 0x5969d0` |
| `0x005968D0` | `0xE1A00000` | `0x1A000001` | `nop` | `bne 0x5968dc` |
| `0x005968D8` | `0xE1A00000` | `0x1A00003C` | `nop` | `bne 0x5969d0` |
| `0x005968E4` | `0xE1A00000` | `0x0A000039` | `nop` | `beq 0x5969d0` |

> 別候補: Vapecord `Walkseeder`（JPN `0x0059689C` `EA000014`＝無条件 `B 0x005968F4`）。1 語だが飛び先が上と違う。
> `花散らせない`（`0x00596890`）と同じ関数を書き換える。**両方 ON にしたときは `歩いた場所のアイテムを消し去る` が優先される**（利用者の確認）。

### どこでも掘れる

| 欄 | 内容 |
|---|---|
| 種別 | トグル式 |
| 説明 | 木/岩やアイテムから、川/崖越し/建造物まで、ありとあらゆる状況で掘れるようになります。 |

**PatchList（JPN・9 語）** — 出典: 旧記載・`REFERENCES/CHEATS/shovel.txt`〈どこでも穴掘れるようになる〉（同じ 8 語）＋旧 `DOCS/patches.md` #9（2026-09-17 利用者指示で有効）。

| 番地 | ON | OFF（元の命令） | ON の命令 | OFF の命令 |
|---|---|---|---|---|
| `0x00595B00` | `0xE320F000` | `0x0A00006F` | `nop {0}` | `beq 0x595cc4` |
| `0x00598294` | `0xEA000095` | `0x0A000095` | `b 0x5984f0` | `beq 0x5984f0` |
| `0x00598478` | `0xEA00001C` | `0x0A000042` | `b 0x5984f0` | `beq 0x598588` |
| `0x005984EC` | `0xE320F000` | `0x0A000019` | `nop {0}` | `beq 0x598558` |
| `0x00598550` | `0xE320F000` | `0x13A0401A` | `nop {0}` | `movne r4, #26` |
| `0x00662E88` | `0xE320F000` | `0x0A000150` | `nop {0}` | `beq 0x6633d0` |
| `0x00663294` | `0xEA000028` | `0x0A000028` | `b 0x66333c` | `beq 0x66333c` |
| `0x00680F10` | `0xE320F000` | `0x0A00000A` | `nop {0}` | `beq 0x680f40` |
| `0x005982C4` | `0xEA000017` | `0x0A000017` | `b 0x598328` | `beq 0x598328` |

> **9 語目 `0x005982C4`**（旧 `DOCS/patches.md` #9、実機確認済み）: 掘る対象を決める関数（旧名 `FieldObj_ResolveDigAction`）で、
> `Item_IsBuriedFlag` の結果による分岐「埋まっていなければ通常の経路へ（`BEQ 0x00598328`）」を無条件 `B` にする。
> 化石・埋めたベル・埋めたアイテムがあるマスでも掘り出しの処理（`0x005982C8` 以降）へ入らず、何も無いマスを掘ったのと同じ扱いになる。
> **副作用: 埋まっていたものは掘り出せず消える。** 利用者指示で有効（2026-09-17）。


---

## 8. root/アイテム/ドロップ/お遊び

### 木にぶつかって切り倒す

> 設計: [§17.6](#176-木にぶつかって切り倒すトグル式フック-1-か所)

| 欄 | 内容 |
|---|---|
| 種別 | トグル式 |
| 説明 | 木や竹にぶつかると切り倒されます。 |

### スコップ3x3マス掘り

> 設計: [§17.7](#177-スコップ3x3マス掘りトグル式フック-4-か所)

| 欄 | 内容 |
|---|---|
| 種別 | トグル式 |
| 説明 | [穴に落下しない] [どこでも掘れる] と併せて使うと面白いかもしれません。 |

---

## 9. root/村

### アイテムが消えない

| 欄 | 内容 |
|---|---|
| 種別 | トグル式 |
| 説明 | 無効な位置にあるアイテムが翌日に消えないようにします。 |
| 由来 | Vapecord |

**未解析**（2026-09-17 利用者判断: 解析が必要）。PatchList で実装できるかも含めて、解析してから決める。

参考（採用していない）: Vapecord `ItemsDontDissappearOnInvalidPositions` はパッチ 2 語（JPN `0x006FA8A8` `E3A00000` / `0x0052734C` `E1A00000`）に加え、
種アイテムを枯らす関数 `0x002FC950` へのフックを使う。フックが働く呼び出し位置は Thumb の `BLX 0x002FC950` の 5 か所
（`0x003159F2` / `0x00315A7A` / `0x00315B08` / `0x00316392` / `0x00316412`）。照合結果は `work/evidence/patchlist/candidates.json` / `candidates2.json`。

解析（2026-09-18、V2-F015、静的 MEDIUM）: 3 部品とも**日替わり処理（しずえ画面の準備中）の中の処理**を止めている。
- パッチ `0x006FA8A8`: アイテム削除 `Field_DeleteItemAt` 0x006FA828 の書き込みを止める。日替わりでは建物の足元・公共事業の完成区画・家の目印の削除に使われる。
- パッチ `0x0052734C`: 建物の足元を押す `Structure_StampFootprint` 0x00526C14 で木など大物を消す枝を止める。
- フック `0x002FC950`（マスへ書く汎用関数）: 無効な位置の苗が枯れる 5 か所だけ、枯れ姿の代わりに 1 段成長させる。
- ただしパッチ 1/2 の関数は日替わり以外からも呼ばれる（例: 夢見の館で他人の町を読み込んだ直後の作り直し `0x005C9C24`、V2-F016）。外周（x=16/95, y=16/79）で木を消す処理は止めない。詳細 `work/evidence/prepare_screen/growup.md`（acnl_disassemble 側）。

### 天気

> 設計: [§17.8](#178-天気連動型リストボックス)

| 欄 | 内容 |
|---|---|
| 種別 | 連動型リストボックス式 |
| 選択肢 | ゲームに任せる / 晴れ / 晴れ（雲あり）/ 曇り / 小雨 / 雨 / 小雪 / 雪（値と根拠は [§17.8](#178-天気連動型リストボックス)。名前は案） |
| 説明 | 天気を変更します。 |
| 由来 | Vapecord |

### 公共事業エディター

| 欄 | 内容 |
|---|---|
| 種別 | トグル式（ホットキーで入れ切りできる） |
| 説明 | 好きな場所に建造物を建てましょう。カメラだけを動かして置く・動かす・消すができ、プレイヤーは動けません。（操作の要約つき） |
| 実装 | `Sources/Cheats/BuildingEditor.cpp` / `BuildingPreview.cpp` / `BuildingHighlight.cpp`、置く・消す・動かすは `PublicWorks.cpp` |
| 実機 | 利用者が確認（2026-09-24）。記録は acnl_disassemble の IDA-opus-5.5-F014〜F022 |

操作（メニューを閉じている間）:

| 操作 | 動き |
|---|---|
| スライドパッド | カーソルを 1 マスずつ（押し続けで連続）。カメラが追う |
| L / R | モード: 配置 → 移動 → 削除 |
| 十字 左右 | 配置する建物を替える（プレビューの読み込みは押す手が止まってから） |
| X | 配置: カーソルの下（無ければ一番近く）の建物の種類をコピー |
| A | 配置: 置く／移動: 選ぶ → カーソルへ動かし選択を外す／削除: 選んだ建物を消す |
| B | 移動: 選択を外す |

挙動:

- **カメラ**: `CameraGame`（`*(0x94A880)`）の基準位置 +4 を毎フレーム書く。エディターの間だけ `0x1A5128` を `POP {R4-R8,PC}`（Vapecord UNLOCKCAMERA と同じ語）
- **プレイヤーを止める**: 入力遮断ケーブ A のモード 3（ボタン＋スライドパッドのビットとアナログ値）
- **UnitCursor**（斜め縞）: 衝突判定のマス = 足元データの「物・花を置けない」マス（**花を植えられない属性** `byte_957A35 == 0`。
  1 マスも無ければ足元の範囲を一回り削った四角）。家など名前の無い建物はゲームが読み込み済みの足元（`Building_GetFootprint 0x1B1720`）。
  高さは全部そろえて設置プレビューと同じ。色: 配置 = そのまま、移動 = 青、削除 = 赤
- **設置プレビュー**（配置モード）: ゲームの公共事業プレビュー `0x227CFC` と同じ組（モデル＋季節 00 のテクスチャ＋LUT）。
  色は合成せず、不透明度を sin 波で揺らす。高さは実際に置くときと同じ（地面、橋は +32）
- **選択**: 衝突判定のマスがカーソルに重なる建物のうち、基点がマンハッタン距離で一番近いもの。移動 = 青、削除 = 赤、
  移動で何も選んでいないときはカーソルを合わせた建物を白（合成度合い 50）。不透明度は sin 波で揺れる
- 重なりは確かめない。置く・消す・動かすの上限・断る条件は §18.3.4（旧 テスト/公共事業）のとおり（ゲームの上限を超えない）
- その場で作れない建物（特殊・枠不足など）は部屋を読み直し、理由を通知する。読み直しのあとはカーソル・モード・種類を保ったまま自動で再開
- 借りるメモリは親ヒープ `*(0x94CC48)` から（プレビュー・UnitCursor）。ゲームの分として 48 KB は残す
- 検査: `tools/strc/verify_building_editor.py`、`tools/models3d/verify_highlight.py`、`tools/strc/verify_publicworks.py`

### 公共事業エディターの設定（フォルダ）

| 項目 | 種別 | 既定 | 説明 |
|---|---|---|---|
| 選択の色の濃さ | 数値 0〜255 | 176 | 移動で選んだ建物の青・削除の赤の濃さ |
| 選択の不透明度 | 数値 0〜255 | 208 | 選んだ建物の揺れの中心 |
| 選択の揺れ幅 | 数値 0〜255 | 64 | |
| 選択の揺れの速さ | 数値 1〜100 | 11 | 1 フレームに周期の 1/1000 ずつ |
| プレビューの不透明度 | 数値 0〜255 | 160 | |
| プレビューの揺れ幅 | 数値 0〜255 | 64 | |
| プレビューの揺れの速さ | 数値 1〜100 | 11 | |

### 空を見上げない

| 欄 | 内容 |
|---|---|
| 種別 | トグル式 |
| 説明 | あなたは勝手に空を見上げて呆けることはありません。 |

**PatchList（JPN・1 語）** — 出典: 旧記載。ゲーム自身の分岐先（`0x0064C558` の `BNE` が飛ぶ `0x0064C514`）へ飛ばす 1 語なので採用。

| 番地 | ON | OFF（元の命令） | ON の命令 | OFF の命令 |
|---|---|---|---|---|
| `0x0064C594` | `0xEAFFFFDE` | `0xE5D4191A` | `b 0x64c514` | `ldrb r1, [r4, #2330]` |

> 別候補: `REFERENCES/CHEATS/camera.txt`〈空を見上げない〉の 4 語（`0x001A1FAC` / `0x001A1FC8` / `0x001A1FEC` / `0x001A6D1C` を `E1A00000`）。元の命令とは一致するが、カメラ側の引数や書き込みを消す形で影響範囲が読めないため採用していない。


---

## 9.5 root/村/マップ

地図で選んだものに対する操作をまとめる（2026-09-18 利用者指示）。R1 により `村` の中ではこのフォルダを先に置く。

### 選択中アイコンに部屋移動

旧名 `選択したマップアイコンのroomに移動`（2026-09-18 改名）。

| 欄 | 内容 |
|---|---|
| 種別 | アクション式 |
| 説明 | 地図上で選択したアイコンのroomへワープします。 |

### 選択した住民への操作

| 欄 | 内容 |
|---|---|
| 種別 | アクション式 |
| 説明 | 地図上で選択した家主の住民に対して様々な操作をします。 |

挙動:

- 住民の元へワープ、プレイヤーの元へワープ、住民を家から出させる、住民を家に帰らす、など。
- 選択していない状態だと、**住民全員**に対して操作する。これを明示的にするため UI で分かりやすく表示する。

### 選択中プレイヤーに切り替え

旧名 `選択したプレイヤーのデータに切り替える`（2026-09-18 改名、`root/プレイヤー` から移動）。**未解析**。以下は仕様。

| 欄 | 内容 |
|---|---|
| 種別 | **トグル型アクション式**（2026-09-18 利用者指示。旧: トグル式） |
| 説明 | もうわざわざタイトル画面へ戻る必要はありません。地図上で選択したプレイヤーのデータへ切り替えます。 |

挙動（仕様）:

- 実行（メニューで ON を適用、またはホットキーの確認を承諾）したとき、地図上で選択中のプレイヤーのデータへ切り替える。
- **発動条件: 通信中でない、かつ村にいるとき。** 条件を満たさないときは切り替えず、その理由を通知する。
- 切り替えの方法（セーブ・プレイヤーの読み込み手順）は未解析。解析してから実装する（R2）。

---

## 10. （欠番）

`root/Shop` は廃止した。`店24時間オープン` は [§12.5 root/ゲーム/店](#125-rootゲーム店) へ移した（2026-09-17 利用者指示）。

---

## 11. root/ゲーム

### メッセージ即表示

| 欄 | 内容 |
|---|---|
| 種別 | トグル式 |
| 説明 | メッセージの字送りを即時に完了させます。しずえやかっぺいの面倒な話を聞く必要はもうありません。 |

**PatchList（JPN・1 語）** — 出典: 旧記載〈会話を一瞬で表示〉。`sub_5F8238`（呼び出し元は `0x005FAF68` の 1 か所）の中で `LDRB R0,[R4,#0xCA]` を 1 にし、ゲーム自身の分岐を通す。関数の処理を飛ばさないので採用。

| 番地 | ON | OFF（元の命令） | ON の命令 | OFF の命令 |
|---|---|---|---|---|
| `0x005F8278` | `0xE3A00001` | `0xE5D400CA` | `mov r0, #1` | `ldrb r0, [r4, #202]` |

> 別候補: `REFERENCES/CHEATS/speed.txt`〈メッセージ速度〉`0x005FAF68` `E3A00001`（呼び出しそのものを 1 に置換）、Vapecord `fasttalk`（JPN `0x005FAF64` `EA000000` / `0x005FAF6C` `E3500001`）。どちらも `sub_5F8238` を実行しなくなるので採用していない。


### しずえスキップ

| 欄 | 内容 |
|---|---|
| 種別 | トグル式 |
| 説明 | タイトル遷移後のしずえの会話をスキップします。ドパガキなあなたへ。 |

> ★**副作用の可能性（静的・2026-09-17、V2-F013）**: 「準備しています」の間の仕事は日替わり（成長）処理と受信ボックスの取り込み（SvProc 種別 4）。
> しずえスキップはこの case 0 を走らせないので、これらを飛ばしている可能性がある（村に出た後の別経路で補われるかは未確認）。日付をまたいで通常経路と実機で比べる必要がある。
> 日替わり処理の中には**掲示板の新しい記事・雑草が生える処理・住民の引っ越し判定・誕生日の手紙**もある（V2-F014）。比べるときはこれらも見る。
> **実機確認（2026-09-17、利用者）**: しずえスキップでは少なくとも**寝癖**と**郵便ポストのメール**が一緒にスキップされる。
> ほかの日替わり処理（掲示板・雑草・引っ越し判定など）も同様にスキップされている可能性が高いが、個別には未確認（断定しない）。

### フレームレート制限解除

| 欄 | 内容 |
|---|---|
| 種別 | トグル式 |
| 既定ホットキー | `B+DPad Up` |
| 説明 | フレームレート制限を取っ払うことで、事実上ゲームの進行速度を上げます。 |

**PatchList（JPN・1 語）** — 出典: 旧 `RE/FINDINGS.md` F-356（HIGH・静的）。Vapecord `speedentry`（USA→JPN 変換）・ROTATION `FastGameSpeed` とも同じ 1 語。前フレームの VBlank カウンタの代わりに巨大な定数を入れ、フレーム待ちに入らなくする。FPS とゲーム進行は切り離せない（F-357）。

| 番地 | ON | OFF（元の命令） | ON の命令 | OFF の命令 |
|---|---|---|---|---|
| `0x0054C6E8` | `0xE3E004FF` | `0xE59400A0` | `mvn r0, #-16777216` | `ldr r0, [r4, #160]` |

> 注意: Vapecord はセーブ中だけこのパッチを外している（理由は未確認）。PatchList だけではセーブ中の切り替えができない。

---

## 12. root/ゲーム/キーボード

### キーボード制限解除

| 欄 | 内容 |
|---|---|
| 種別 | トグル式 |
| 説明 | 文字数制限、改行、無効化されたキーの有効化など、キーボードに関する制限を撤去します。 |
| 由来 | Vapecord |

**未解析**（2026-09-17 利用者判断: 解析が必要）。

参考（採用していない）: Vapecord の制限解除系は `.bss` の 1 バイト（JPN `0x00AD0253` = 1〈KEYENTER〉/ `0x00AD05C0` = 1〈KEYAT〉/ `0x00AD0158` = 2〈チャットの数字制限〉）と、
開いたキーボードの動的データ（`[[BaseInvPointer]+0xC]+0x1328` の `+0x0C` / `+0x12B`）への書き込み、`CustomKeyboard` のキー表 `0x00AD0630` への書き込みで成り立つ。
`.bss` なので元の値は code.bin から読めない。照合結果は `work/evidence/patchlist/candidates2.json`。

### 漢字変換

| 欄 | 内容 |
|---|---|
| 種別 | トグル式 |
| 説明 | 漢字変換が出来るようになります。 |

挙動:

- **内蔵キーボードで使われる漢字変換アルゴリズムを呼び出して使用する。**
- 【実装済み・実機未確認（gohan work/sim-0925、2026-09-25）】実装は `Sources/ChatKanji/ChatIme.cpp`、根拠は解析の IDA-opus-5.5-F043 / F044。
- ON の間、**普通のチャット**を開いていると、下画面に候補欄を描く（Simulator 670e44f の `chat-kanji-preview.js` と同じ位置・色:
  x 8 / y 49 / 304x17、地 `#522810`、文字 `#fff3d6`、選択 `rgba(239,255,214,.24)`、はみ出しの印）。
  字形はゲームの `Garden_msg_size16` を 0.72 倍（要求の大きさ = FINF 幅 18・高さ 24 の 0.72 倍）。下画面に自前の UI（リストボックスなど）が出ている間は描かない。
- **候補は自動で取る**（利用者の指示 2026-09-26。当初のホットキー方式はやめた）: 変換の対象（未確定の文字列、無ければ入力欄の選択範囲）が
  **100ms 変わらなかったら**（当初 300ms。変換が速くなったので 2026-09-26 に短くした）候補を取って候補欄に並べる。取っただけでは入力欄は変えない。同じ対象は取り直さない。
- 候補欄で選ぶと、その候補が入力欄に入る（未確定のまま）。選び直すたびに入れ替わる。選び方は十字キーの左右（未選択から右で 1 番目・左で最後。
  **長押しで連続**: 200ms 後から 60ms ごと。メニューの長押しと同じ）と候補のタッチ。
  **選択中の候補をもう一度タッチして、あまり動かさずに離すと Enter**（4px 以上動かしたらスクロールとみなして決定しない）。
  選択範囲の候補を選んだときは、その範囲が未確定の文字列になる。
  候補欄はタッチで横に動かせる。候補が出ている間はゲームの十字キーを止める（カーソルが動いて確定しないように）。欄の上で始めたタッチもゲームへ渡さない。
  選ぶ前に字を打てば候補は消え、止まってからまた取る。
- **配置**（Simulator 639b4e6 `docs/chat-kanji-layout.md`）: 変換行はキーボードの幅（x1..318、y49..67、高さ 19）で、キーボードの丸い上端を覆う。
  候補欄はその左（x1..278）、右端の列（x279..318、「消去」の真上）が **「全選択」キー**（Simulator の「クリア」。利用者の指示で機能も文字も全選択）。
  **左右キー**（24x22）は右上（x271 / 295、y1。入力欄には重ねない）。行の縁は `#3a1d0c`。
- **自前キーの見た目はゲームのキー**: 「ABC」「あいう」などの切り替えキー（`swkbd_common.arc` の `KeytopModeSelect`、中央の部品 `KtpModeC`）を借りる。
  かなのキーの部品（`swkbd_kana.arc`）はかなキーボードの間しか読み込まれないので使わない。テクスチャはキーボードのリソースアクセサ（BsSkb+800）から
  ゲームのスレッドで取り（読み込み済みの控え）、Material の色[0]・頂点色・文字色もレイアウトの値をそのまま使う。押している間は切り替えキーの
  「選択中」の見た目（`KtpModeCon` と桃色の頂点色・文字色、`KeytopModeSelect_n0s1` のフレーム 1）。キーの上で離すと実行し、
  **`SE_SYS_SWK_CHANGE_KEY_SET`**（「ABC」「あいう」を切り替えたときの音）をゲームの処理で鳴らす。
  - 全選択: 途中の入力を確定し、起点 0 から末尾まで選択（`TextManager_SetCursor`）。左右: 選択中なら選択の端へ、そうでなければ 1 字動かす。
- **チャットの開閉アニメに追従**: キーボードの BG レイアウトの `N_All`（`BG_in` / `BG_out` が動かす。ゲーム自身もこの値をキーボードの各レイアウトへ写している）の
  位置を毎回読み、変換行・キーを同じだけずらす（メニューのスレッドから読むので、最大 1 フレーム遅れうる）。
- **Enter で確定**（ゲーム自身の「未確定を確定」の処理。送信ボタンでも確定してから送る）。候補を選んだあとの **Backspace で変換前の読み（未確定）へ戻る**
  （候補はキャッシュからすぐ出す）。候補を選んだあとに字を打つと、その候補で確定して次の未確定を始める。
- **この項目のホットキーは Enter の代わり**（利用者の指示 2026-09-26）。ゲームの Enter キーと同じ処理（字 10 の入力）を呼ぶ。
- **キャッシュ**（利用者の指示 2026-09-26）: 変換の対象（読み）とその候補の一覧を **255 件まで**持つ。溢れたら一番長く使っていないものから消す。
  キャッシュにあるものを使ったら「いま使った」に更新する（よく使うものは消えない）。合計の大きさも 1 MiB までに抑える（普段は 1 件数百バイトなので 255 件が先に効く）。
  チャットを閉じても消さない（プラグインを終えるまで）。
- **エンジンの下準備を 1 回にした**（2026-09-26。速くするため）: 以前は変換のたびに作業領域 5 MiB の確保・内蔵タイトルからの辞書 2.0 MB とコード 0.47 MB の読み込み・
  展開・再配置・言語の初期化をやり直していた。いまは**普通のチャットを開いたとき**に裏で 1 回だけ済ませ、変換ごとには変換と候補の列挙だけを行う。
  チャットを閉じたら作業領域を返す。使い回しても候補が変わらないことは、エミュレーターで毎回新しく変換した結果（元のエンジン）と突き合わせて確認した
  （解析 repo `tools/chat_kanji/emulate_resident.py`。準備 1 回 + 変換 9 回、全件一致）。
- 未確定の文字列はゲームの入力欄の「ローマ字の未確定」と同じ欄（TextManager+0x24）で持つので、飾り・確定・削除はゲームの処理のまま。
- Simulator との差: 切り抜きが無いので、はみ出した候補の文字は描かない（選択の塗りは欄の内側で切る）。字ごとの送りは GPU と同じ 11.52px（Simulator は字ごとに 12px に丸める）。
  変換エンジンの処理中は候補欄に「変換中」と出す（Simulator には無い）。

### コンポジション

| 欄 | 内容 |
|---|---|
| 種別 | トグル式（漢字変換の下） |
| 説明 | かな入力に未確定の文字列を付けます。漢字変換はこの未確定の文字列を変換します。 |

挙動（【実装済み・実機未確認】`ChatIme.cpp`）:

- **漢字変換が ON のときだけ効く。** 普通のチャットのかなキーボード（gui::KanaKeySet）で打った字を未確定の文字列にする（ゲームのかな入力には未確定が無い。F044）。
- Enter・送信・カーソル移動・空白で確定。Backspace は未確定の字を 1 字消す。ローマ字の入力へ切り替えて打つと、かなの未確定は先に確定する。
- OFF のときは今までどおり 1 字ずつ確定する。変換したいときは入力欄で文字を選択する（300ms 後に候補が出る）。

---

## 12.5 root/ゲーム/店

`ゲーム` フォルダの `キーボード` の下隣に置く（2026-09-17 利用者指示）。

### 店24時間オープン

| 欄 | 内容 |
|---|---|
| 種別 | トグル式 |
| 説明 | 全てのお店は常にあなたのために回り続けます。 |
| 由来 | Vapecord |

**PatchList（JPN・9 語）** — 出典: Vapecord `ShopsAlwaysOpen`（USA→JPN 変換）。9 語とも `MOV R0,#0` → `#1`。候補はこれだけ。

| 番地 | ON | OFF（元の命令） | ON の命令 | OFF の命令 |
|---|---|---|---|---|
| `0x003093BC` | `0xE3A00001` | `0xE3A00000` | `mov r0, #1` | `mov r0, #0` |
| `0x007102C8` | `0xE3A00001` | `0xE3A00000` | `mov r0, #1` | `mov r0, #0` |
| `0x00710380` | `0xE3A00001` | `0xE3A00000` | `mov r0, #1` | `mov r0, #0` |
| `0x00712664` | `0xE3A00001` | `0xE3A00000` | `mov r0, #1` | `mov r0, #0` |
| `0x0071BBE0` | `0xE3A00001` | `0xE3A00000` | `mov r0, #1` | `mov r0, #0` |
| `0x00710000` | `0xE3A00001` | `0xE3A00000` | `mov r0, #1` | `mov r0, #0` |
| `0x0071DE08` | `0xE3A00001` | `0xE3A00000` | `mov r0, #1` | `mov r0, #0` |
| `0x0071684C` | `0xE3A00001` | `0xE3A00000` | `mov r0, #1` | `mov r0, #0` |
| `0x00716BF8` | `0xE3A00001` | `0xE3A00000` | `mov r0, #1` | `mov r0, #0` |

> **パッチ数を減らせるかの静的調査（2026-09-17）**: 減らせない。営業時間の判定は店ごと・用途ごとの関数に複製されていて共通の入口が無く、9 語はそれぞれ別の関数の「閉店（0）を返す出口」なので関数ごとに 1 語が下限。
> 時刻は全体の日時（`Date_GetRaw` `0x002FB3A4` → `0x00ACF6B0`、466 か所から参照）、条例は `sub_7585A0` / `sub_7585C4` で、どれも 24 時間に固定できる形ではない。
> ★この 9 語が触らない同種の判定がある（V2-F012。IDB 名で記す）:
> `ShopHours_ReTail_InHours` `0x003093F8`（Re-Tail の営業時間だけ。住民が Re-Tail へ出かけられるかの判定と ModuleShop の条件分岐が使う）、
> `ShopHours_Ables_IsOpen2` `0x007123E8`（エイブルの判定と挙動が同一。住民がエイブルへ出かけられるかの判定だけが使う）、
> `ShopHours_Redd_InHours` `0x00716860`（つねきちの店の営業時間だけ。ModuleIndoor が使う。用途未解明）、
> 看板や BGM 用と推定される番号の選択 `sub_70FDD8` / `sub_71BC24` / `sub_71DBCC` / `sub_717088` など。実機で不整合（店内で閉店扱い・閉店の BGM など）が出るなら、ここを追加するか、`Date_GetRaw` を呼び出し元で絞って 12 時を返すフック（1 か所・語数は増える）を検討する。
> 根拠と一覧: `work/evidence/shop_open/README.md`・`callers.json`（`tools/patches/shop_open_callers.py`）。

### カブ価

| 欄 | 内容 |
|---|---|
| 種別 | 連動型数値入力・整数（0〜99999） |
| 説明 | カブ価を変更します。 |
| 由来 | Vapecord |

店のカブの値段で、プレイヤーの資産ではない（2026-09-17 利用者確認。以前は `資産` の `カブ` だった）。

**設計（2026-09-17 の静的な読み取り。実機未確認）** — 暗号化された値の読み書きは [§4](#4-rootプレイヤー資産) と同じ関数（`DecryptValue` `0x00303530` / `EncryptValue` `0x00303404`）を使う。

| 場所 | 根拠 |
|---|---|
| セーブ全体の先頭 `[0x00955F8C]` +0x6ADE0 から 12 個（午前 6・午後 6） | Vapecord `TurnipPrices` の注記（全体の先頭から）。同じ注記の `ShampoodleUnlockStatus` 0x6ADB4 をゲームが `0x0017109C` で読んでいて、注記の基準が一致 |

- 読み取り: メニューを開いた時に 1 回、1 個目を読む。検査和が合わない・セーブが無い・範囲外なら編集できなくする（カーソルは置ける）。
- 書き込み: 適用で 12 個とも同じ値にする。
- 未確認: 1 個目（曜日・午前午後のどれか）を表示値にしてよいか、店に反映される時機。

---

## 13. 既定ホットキー一覧

| ホットキー | 項目 |
|---|---|
| `A` | 座標移動 |
| `R+B` | チャットコマンド |
| `R+DPad Up` | 歩いた場所のアイテムを消し去る |
| `L+DPad Up` | 壁抜け |
| `L+DPad Down` | アクション解除 |
| `B+DPad Up` | フレームレート制限解除 |

---

## 14. チャットコマンド

`root` の `チャットコマンド` が ON のとき、ゲーム内のチャット欄に打てるコマンド。

記法: `<>` は必須、`[]` は省略可、`|` は選択肢。

### 入力の振り分け

チャット欄の入力は次の順に判定する。

1. **コマンド名に完全一致**すれば、そのコマンドとして実行する。
2. 完全一致せず、**数字または `A`〜`F` で始まる**なら [`<ITEMID>`](#itemid) として扱う。
3. **かな入力**なら [`<ITEMNAME>`](#itemname) として扱う。**かな文字のコマンドを追加する予定はない。**

### 日付・時刻の書式

- **日付の区切りは `-`（ハイフン）または `/`（スラッシュ）**。両方を受け付ける。
- 時刻の区切りは `:`。`HH:MM` は 24 時間制。
- 時刻・日にちの直接指定は**先頭ゼロを省略できる**。

### `list`

- チャットコマンドを**下画面リストボックス**で全て表示する。
- 選択すると、**確定している文字のみ**チャット欄に入力する。

### `time set [[[yyyy-]MM-]dd | [HH:]mm | day | night | noon | midnight]`

- `set` だけの場合、**時刻設定メニューを呼び出す**。
- 別名: `day`=7:00、`night`=18:00、`noon`=12:00、`midnight`=0:00
- 由来: Foxxy

### `time <add|sub> [[[yyyy-]MM-]dd | [HH:]mm]`

- 指定した分だけ加算・減算する。

### `<ITEMID>`

- 存在するアイテムであれば、持ち物の空いたスロットに入れる。
- テキストには**アイテム名と入ったスロットの index** を表示する。
- `ポケットアイテム` と同じメソッドを使う（**未解析**。[§17.5](#175-ポケットアイテムアクション式--16-進数入力)）。

### `<ITEMNAME>`

- 日本語でアイテムを**部分一致**検索する。
- 一致したアイテム候補を**下画面リストボックス**として表示する。
- 選択したアイテムを持ち物の空いたスロットに入れる。
- `ポケットアイテム` と同じメソッドを使う（**未解析**。アイテム名の検索もゲーム自身のデータを使う前提で解析する）。

### `weather <clear|rain|cloudy>`

- `天気` と同じメソッドを使う。

### `<hair|style|eye[s]>`

- `スタイルを変更`と同じ専用カスタムメニューを開く。

---

## 15. 別途詰める

この仕様書では扱わず、**GUI・メニュー設計の仕様**として別に決める。

振る舞いの仕様は [gohan-menu.md](gohan-menu.md) へ分けた。

| 項目 | 状態 |
|---|---|
| **トグル型アクション式の実装形**（`気絶`） | **解決済み**（2026-09-17）。自前メニューに項目種別「トグル型アクション」として実装し、main（`3389f71`）で利用者が実機確認。ON を適用すると `RegisterExecute` の関数を 1 回呼んで OFF へ戻り、値欄に `OK` を 800ms 表示。ホットキーは確認ダイアログの承諾後に 1 回実行（[gohan-menu.md §5.5](gohan-menu.md#55-トグル型アクション式確定)） |
| **既定ホットキーの割り当て方法**（[§13](#13-既定ホットキー一覧) の 6 件） | 未解決。初期値として焼くか、利用者が束縛するか（[gohan-menu.md §9](gohan-menu.md#9-未定) の 4） |
| **`スタイルを変更`専用カスタムメニューの詳細UI** | 未解決。設定・実行はそのメニュー内で行い、選択中の見た目画像も表示する（→ [§5](#5-rootプレイヤースタイル)）。画面配置と操作は別途決める |

## 16. 未定

- `ドロップアイテム` を参照する「ドロップ系チート」（[§7](#7-rootアイテムドロップ)）。今後追加するチートで参照する。
- `スタイルを変更`と専用メニュー内の各設定、`アイテムセレクター`の説明文（[§5](#5-rootプレイヤースタイル) / [§7](#7-rootアイテムドロップ)）。あとで追加する。

## 17. PatchList 以外のチートの設計

2026-09-17 の設計案。**R2 により、ここに書いた番地・関数の使い方は実装前に解析して確かめる。**
番地はすべて JPN。Vapecord の USA 番地は同梱の `Addresses.hpp`（JPN 列）で変換し、code.bin の命令を確かめた
（`tools/patches/design_addresses.py` → `work/evidence/patchlist/design_addresses.json`）。
項目の振る舞いの型は [gohan-menu.md](gohan-menu.md) の §4 / §5。

### 17.0 共通で使うもの

| 用途 | JPN 番地 | 出典・確度 |
|---|---|---|
| プレイヤーオブジェクト取得 `GetPlayer(pIndex, 1)` | `0x005C27D8` | Vapecord `PLAYERINSTANCE`（`Call(pIndex, 1)`）。未解析 |
| 自分のプレイヤー番号 | `0x00305F6C` | Vapecord `A_GETONLINEPLAYERINDEX`。未解析 |
| ワールド座標（マス）取得 `GetWorldCoords(&x, &y, pIndex, 1)` | `0x005BFCE4` | Vapecord `WORLDCOORDS`。旧 F-62 でも `vc_WORLDCOORDS` として使用 |
| 座標 X / Y / Z（float×3） | `player+0x14` / `+0x18` / `+0x1C` | 旧 `DOCS/structures.md`・Vapecord `PlayerClass` が一致 |
| 現在の状態 ID | `player+0x1A9` | 旧 F-66（`+0x1AA` が直前状態） |
| アニメーション（状態遷移）パケット取得 `GetAnimInst(player, 0, 0, 0)` | `0x006561F0` | 旧 F-66 `vc_GETANIMINST` と Vapecord `GETANIMINST` の変換結果が一致 |
| 状態遷移 `Actor_SetState(player, 状態, packet, 0)` | `0x0064C688` | 旧 F-54（Vapecord `ANIMFUNCTION`）・F-59・F-66。状態表 `0x00843634`、刻み `0x1C` |
| マス → ワールド座標 | `32 * n + 16` | Vapecord `GetCoordinates(wX, wY)`・旧 `cave_particle.py`（`sub_6A68E4`）が一致 |

- 状態を変える処理は**ゲームのスレッドで**行う（Vapecord の注記。メニュースレッドから直接呼ばない）。gohan では描画フック（`RenderTop` など）と同じくゲーム側の周期で 1 回だけ実行する仕組みを使う。
- オンライン時の同期（Vapecord はパケット送信 `0x005C25B4` を併用）は**今回の設計の対象外**。まずオフラインで成立させる。

### 17.1 座標移動（押下中実行型）

| 項目 | 設計 |
|---|---|
| 型 | [gohan-menu.md §4.1](gohan-menu.md#41-押下中実行型--押している間だけ) の押下中実行型。`ToggleHandlers::OnTick` で毎フレーム |
| 発動条件 | 項目 ON、かつホットキー（既定 `A`）を押している間。メニュー・入力 UI 表示中は `held` が 0 なので止まる |
| 対象 | `GetPlayer(自分の番号, 1)` の `+0x14`（X）と `+0x1C`（Z）。`+0x18`（高さ）は触らない |
| 十字キー | 右 = X+ / 左 = X− / 下 = Z+ / 上 = Z−（Vapecord と同じ向き）。1 フレームの移動量 = `移動量` |
| スライドパッド / C スティック | `hidCircleRead` / `hidCstickRead` のベクトルを使い、向きはそのまま（8 方向に丸めない）、傾きの割合（最大値で正規化）×`移動量`。Y は符号を反転して Z に足す。**直接読むので `GuiMenu::IsVisible()` の間は自前で止める** |
| グリッド単位 | 現在マス（`GetWorldCoords`）から入力方向へ 1 マス進んだマスの中心（`32n+16`）へ置く。押し続けたときは `CONTROL_REPEAT` と同じ 200ms 待ち・60ms 間隔で 1 マスずつ。スライドパッドは 8 方向に丸める |
| 止める条件 | プレイヤー未ロード（オブジェクトが 0）、スコップ動作中（状態 `0x4F` / `0x52`。Vapecord のタッチワープと同じ除外） |
| 十字キーの無効化 | 子設定 `十字キーの無効化` が `座標移動中` かつ移動キーが十字キーなら、ホットキー押下中は `GuiMenu::BlockGameDpad()` でゲーム側の十字キーを止める（入力遮断ケーブの値 2） |
| 未解析 | 既定ホットキー `A` はゲームの A と重なる（押すと話しかけ等も起きる）。当たり判定・部屋外への移動の扱い |

### 17.2 タッチワープ（トグル式・毎フレーム）

| 項目 | 設計 |
|---|---|
| 型 | トグル式。`OnTick` で毎フレーム。ホットキーを設定していれば押している間だけ有効（未設定なら常に） |
| 発動条件 | 地図が開いている（`[0x00949C30]` が非 0。Vapecord `MAPBOOL`）かつ下画面をタッチしている。スコップ動作中は除外 |
| 座標変換 | Vapecord `PlayerClass::CalculateCoordinates` の表をそのまま仕様にする（タッチ px → X / Z） |
| 書き込み | `player+0x14`（X）と `+0x1C`（Z）。高さは変えない |
| gohan 側の注意 | 自前の下画面 UI が出ている間はタッチ遮断中なので地図も反応しない。**自前 UI の表示中は動かさない**（`IsVisible()`） |

| 部屋 | タッチ範囲（x, y, 幅, 高さ） | X | Z |
|---|---|---|---|
| 村 `0x00`・情報パネル閉 | `70, 32, 180, 175` | `(tx − 30) × 14.2` | `(ty − 5) × 14.2` |
| 村 `0x00`・情報パネル開（`[[0x00949C30+0x1C]+0x5D8]`） | `7, 32, 180, 175` | `(tx + 33) × 14.2` | `(ty − 6) × 14.2` |
| 島 `0x68` | `76, 36, 168, 168` | `(tx − 72) × 12.1` | `(ty − 23) × 12.1` |
| 商店街 `0x01` | `4, 43, 312, 159` | `(tx + 16) × 6.2` | `(ty − 55) × 6.2` |
| ツアー `0x69`〜`0x7F` | `65, 34, 190, 170` | `(tx − 24) × 13.5` | `(ty + 7) × 13.5` |

★係数はすべて Vapecord の値で、JPN の地図レイアウトと一致するかは未確認（実機で 1 点ずつ確かめる）。

### 17.3 アクション解除（単発実行型）

| 項目 | 設計 |
|---|---|
| 型 | [gohan-menu.md §4.2](gohan-menu.md#42-単発実行型--押すたびに同じ処理を-1-回) の単発実行型。項目 ON のときホットキー（既定 `L+DPad Down`）を押すたびに 1 回 |
| 処理 | 自分のプレイヤーを **Idle（状態 `0x06`）へ遷移**させる。ゲームのスレッドで `packet = GetAnimInst(player, 0, 0, 0)` → 座標を詰める → `Actor_SetState(player, 0x06, packet, 0)` |
| 根拠 | 旧 F-66: 状態 6 は neutral / wait。Vapecord `Animation::Idle` は同じ状態 6 を `GetWorldCoords` のマスで実行している |
| 未解析 | パケットに詰める欄（Vapecord は `COORDOFFSET` `0x005D3504` で座標を書く）。旧 F-66 の「AR 状態キック」（直前状態 13 を置いて 6 へ入る）との違い |

### 17.4 気絶（トグル型アクション式）

| 項目 | 設計 |
|---|---|
| 型 | [gohan-menu.md §5.5](gohan-menu.md#55-トグル型アクション式確定)。ON を適用すると 1 回実行して OFF へ戻り `OK` を表示。ホットキーは確認ダイアログの後に 1 回 |
| 処理 | アクション解除と同じ経路で状態 **`0x9D`** へ遷移させる（Vapecord `Faint` が `ExecuteAnimationWrapper(自分, 0x9D, …, x, y)`） |
| 未解析 | 状態 `0x9D` のパケットに要る欄。状態表 `0x00843634 + 0x9D×0x1C` の記述子で確かめる |

### 17.5 ポケットアイテム（アクション式 → 16 進数入力）

**未解析**（2026-09-17 利用者判断）。

理由: アイテム ID → アイテム名の表示や、ID が存在するかの判断を**資料ファイル（`REFERENCES/IDS` の一覧）に頼る設計は冗長**。
ゲーム自身が持つアイテムの表・名前の取得方法を解析し、それを使う設計にしてから決める。

参考（採用していない。解析の出発点）:

- 持ち物 16 枠の先頭は `vc_GET_ITEM`（`0x00723854`）が `Inventory_GetSlot`（`0x00723878`）へ渡す `sub_2FB900() + 27600`（旧 F-63。4 バイト × 16、空は `0x7FFE`）。
- Vapecord `Inventory::WriteSlot` は持ち物とロック配列（`InventoryItemLocks[slot]`）を書き、持ち物を開いているときは 16 枠の再描画（USA `0x26DC00`。JPN は未変換）を呼ぶ。

### 17.6 木にぶつかって切り倒す（トグル式・フック 1 か所）

旧 F-40〜F-42（**HIGH・実機で完成**）と `reference/old_project/PATCHES/cave_tree.py` をそのまま仕様にする。

| 項目 | 設計 |
|---|---|
| ON | `0x0059BBB0` の `BL FieldObj_FellObject`（元 `0xEBFFF93F`）を自前の処理への `BL` に置き換える。**パッチはこれ 1 語だけ** |
| OFF | `0x0059BBB0` を `0xEBFFF93F` に戻す |
| 自前の処理 | 1. スタック引数 `kind` が 0（ぶつかった）以外は元の `FieldObj_FellObject` へ素通し（揺する `kind=2`・斧は素のまま）<br>2. そのマスの打撃カウンタ `dword_9AE504`（2 bit / マス、`t = X + 80Y − 0x510`、`word=[t>>4]`、`shift=(t&0xF)×2`）を 3 にする<br>3. `Player_BeginFieldObjAction(X, Y, 1)` を呼ぶ（mode 1 は素で斧の解決へ行く）<br>4. キューの `item1` を `a2` に、`flags` bit1 から求めた 4/5 を `kind` にして `FieldObj_FellObject` を呼ぶ<br>5. **キューの `[14]` / `[15]` を解放する**（しないと 4 枠が埋まり斧も効かなくなる） |
| gohan での置き場 | 旧版は CTRPF の空ページ `0x01E81200` に 40 語のケーブを置いた。gohan では**プラグイン内の固定の実行可能領域に同じ語列を置く**（生成器で作り、全分岐・リテラルを監査してから書く）。C++ に書き直さない |
| やってはいけないこと | `0x00595B1C` を `0A000082`（揺すっただけで倒れる）、`0x00597D94` を NOP（斧まで一撃）、レコード登録方式（32 件超で範囲外書き込み。F-39） |
| PatchList との関係 | ON/OFF は 1 語の貼る/剥がすだが、**書く前に自前の処理を置く必要がある**ので PatchList 単体ではない |

### 17.7 スコップ3x3マス掘り（トグル式・フック 4 か所）

旧 F-24〜F-34（**HIGH・実機で完成**）、`cave_3x3.py`（v14）・`cave_particle.py` を仕様にする。

| フック | 元の命令 | 役割 | 旧ケーブ |
|---|---|---|---|
| `0x005A09EC` `BL FieldObj_SpawnActor` | `0xEBFFE245` | 周囲 8 マスにも穴を空ける（`Field_SetItemAt` を使う。F-33） | 42 命令（`0x0066336C`〜） |
| `0x0065BD58` / `0x0065BDEC` `BL sub_66F584` | `0xEB004E09` / `0xEB004DE4` | 抉れパーティクル（刺す / 掘り起こす） | 15 命令（`0x006632F0`〜） |
| `0x0065BD2C` `BL Effect_DigDirt` | `0xEB041950` | 土しぶきパーティクル | 19 命令（旧 `0x01E81000`） |

- どのフックも「中心マスを処理している当人」で元の呼び出しを実行し、dx/dy の 2 重ループで周囲 8 マスを回す（中心は除外）。**手前の段階でフックするとタイミングがずれる**（F-31）。
- `0x005A09EC` の呼び出し元は戻り値を `EOR R5,R0,#1` で使うので、**中心マスの戻り値を退避して戻す**。
- **`どこでも掘れる` には依存しない**（2026-09-17 確定）。旧ケーブの置き場 `0x0066336C` / `0x006632F0` は `どこでも掘れる` の `0x00663294`（無条件 `B`）で使われなくなる領域だったので、gohan ではそこを使わずプラグイン側の実行可能領域に置く。
- ON の順序: 自前の処理を置く → 4 か所の `BL` を書き換える。OFF は逆順（フックを戻してから処理を消す）。
- 1 回の 3x3 掘りでアクタープール（`dword_98447C`、550 個）を 9 個使う。連続で掘ったときの枯渇は未確認。

### 17.8 天気（連動型リストボックス）

| 項目 | 設計 |
|---|---|
| 型 | [gohan-menu.md §5.4](gohan-menu.md#54-連動型確定) の連動型リストボックス。メニューを開いた時に 1 回読み、適用で書く |
| 仕組み | 天気を返す箇所 `0x0062E728`（元 `0xE1A00004` `MOV R0,R4`、直後が `POP {…,PC}`）を `MOV R0,#n`（`0xE3A00000 + n`）に置き換える。Vapecord `Weathermod`・JOKER `ChangeWeather` とも同じ番地 |
| 読み取り | `0x0062E728` の語が `0xE3A00000`〜`0xE3A00006` ならその選択肢、元の命令なら「ゲームに任せる」 |
| 書き込み | 選んだ値を書き `FlushMemory`。「ゲームに任せる」は元の命令へ戻す |

**天気の値**（2026-09-17 の静的な読み取り。実機での見た目は未確認）

| 値 | 選択肢名（案） | 根拠 |
|---:|---|---|
| — | ゲームに任せる | 元の命令に戻す（利用者確定で追加） |
| 0 | 晴れ | JOKER「晴れ(雲なし)」・Vapecord SUNNY |
| 1 | 晴れ（雲あり） | JOKER「晴れ(雲あり)」 |
| 2 | 曇り | JOKER「くもり空」・Vapecord CLOUDY |
| 3 | 小雨 | JOKER「雨(小)」・Vapecord RAINY |
| 4 | 雨 | JOKER「雨」・Vapecord は STORMY（嵐）と呼ぶ |
| 5 | 小雪 | JOKER「雪(小)」・Vapecord SNOWY |
| 6 | 雪 | JOKER「雪」 |

- 天気を返す関数の中（`0x0062E714`〜`0x0062E724`）で、ゲーム自身が条件付きで **3 → 5、4 → 6** に置き換えている（雨を雪に読み替える処理と読める）。
  JOKER の名前（雨(小)→雪(小)、雨→雪）と一致するので、**雪は天気の値として存在する**。
- **嵐という独立した値は見つからなかった。** Vapecord が「嵐」と呼ぶ 4 は、置き換えの対応から「雨（強い方）」と読める。
  このため `嵐` は選択肢に加えない（`雨` として扱う）。実機で 4 の見た目が嵐に見えるなら名前を見直す。
- 通常は値 0〜6 を日付・時刻の表（`0x00957FB4` から引く）で決めている。値 7 は別扱いの分岐がある（意味は未解析）。
- 値を固定すると上の「雨→雪」の置き換えも通らない（冬に 3 を選ぶと雨のまま）。
- JOKER は同時に `0x00949530` へ `値 << 24` も書いている。役割は未解析なので採用しない。
- チャットコマンド `weather` の引数は選択肢に合わせて見直す（→ [§14](#14-チャットコマンド)）。

## 18. root/テスト

解析で確かめたことを実機で試すための場所。**確かめ終わったものから本来のフォルダへ移す。**
ここに残っているものは「実機で通ったが、置き場所と操作がまだ決まっていない」という意味。

フォルダ（2026-09-24 利用者指示）: **UnitCursor**（グリッドカーソルの項目）と **モデル**（モデルビューアの項目）。
**プレイヤー複製**（2026-09-24、試験。§18.3.5）を足した。
公共事業の項目（置く・選ぶ・消す・足元へ・当たり判定を作り直す・選んだ建物を光らせる）は削除し、
`root/村/公共事業エディター` に置き換えた。下の §18.3.4 はその仕組みの記録として残す。

| 項目 | 種別 | 説明 |
|---|---|---|
| グリッドカーソル | チェック | 村の地面にグリッドカーソルを出す。プレイヤーの足元が基点 |
| 横のマス数 | 数値 | 1〜8。画面の横方向 |
| 縦のマス数 | 数値 | 1〜8。横×縦 ≤ 64 |
| 1マスの間隔 | 数値 | マスの間隔＝十字キー 1 回の移動量。world 単位。1〜400、既定 12 |
| カーソルの拡大率 | 数値 | カーソル自身の大きさ。百分率。5〜1000、既定 100 |
| マス目に合わせる | チェック | 出した瞬間に基点をマスの中心へ丸める。既定 ON（表示も ON。[gohan-menu.md §6(a)](gohan-menu.md#a-registertoggleeffect--チェック項目の効果の-onoff)）|
| 縞模様を 45 度傾ける | チェック | 縞を斜めにする。大きさは変わらない。切り替えると組み直す |
| 十字キーで動かす | チェック | 有効な間、十字キーで 1 マスずつ動かす。ゲーム側の十字キーは塞ぐ |
| 状態を見る | 実行 | いまどの段で止まっているかを通知で出す |
| 一覧を作る | 実行 | ゲームの RomFS を歩いて .bcres の一覧を作る。最初に 1 回 |
| 一覧の位置 | 数値 | 一覧の窓の先頭。項目は 255 件までしか持てないので窓をずらす |
| モデル | リスト | 出す .bcres。選ぶとその大きさに合わせて資源ヒープを取り直す |
| モデルを出す | チェック | プレイヤーの足元へ 1 体 |
| ファイル内の番号 | 数値 | 1 つの .bcres にモデルが複数あるときだけ意味を持つ（140 ファイル） |
| モデルの大きさ | 数値 | 百分率 |
| X / Y / Z ずらし | 数値 | プレイヤーからの相対。world 単位 |
| モデルの状態 | 実行 | どこで止まっているか |
| 複製を出す | チェック | 自分と同じ見た目の複製を 1 体、プレイヤーの右隣に出す（§18.3.5）|
| 複製の髪型 | 数値 | -1〜33。-1 = 本物のまま（本物の値を毎フレーム写す）。0〜16 男の子 / 17〜33 女の子。作ったあとも効く |
| 複製の髪色 | 数値 | -1〜15。-1 = 本物のまま |
| 画面に固定 | チェック | 複製を世界ではなく上画面の決まった位置に、専用のカメラで描く（§18.3.5）|
| 複製の向き | 数値 | -180〜180 度（15 刻み）。画面に固定のとき |
| 複製の傾き | 数値 | -90〜90 度（5 刻み）、既定 10。X 軸まわり。正で頭が手前（専用カメラ側）へ |
| 画面の X / 画面の Y | 数値 | 上画面のピクセル（0〜400 / 0〜240）。専用カメラの中心。既定 330 / 150 |
| 複製の大きさ | 数値 | 百分率 10〜300、既定 60 |
| 複製の状態 | 実行 | 段・フレーム数・複製の髪型/髪色・部品の数・late pass で描いたメッシュ数、失敗なら理由 |

### 18.1 大きさは 1 マスにつき 1 体で作る

**ゲーム自身の模様替えカーソルは面積を変えていない**（IDA-opus-5-F028）。

- 家具 1 つにつきカーソル 1 つ。毎フレーム更新（`ModuleIndoor.cro 0x00B1A15C`）が組む行列は
  **対角 1.0 の単位尺**で、そこへ家具の向きの Y 軸回転（`Matrix3x4_AppendRotationY16 0x005669A0`）を掛けるだけ。
- 4 コマの `UnitCursorRotate` は「範囲の形」ではない。メンバは
  `Materials["m_UnitCursor"].TextureCoordinators[1].Rotate` の**1 つだけ**で、値は π/4 と 3π/4。
  **32x32 の `UnitCursorLine` テクスチャの斜線の向き**を振っている。
- そのフレームを決める `Angle16_ToQuadrant 0x00690B68` の引数も**アイテム ID ではなく家具の 16bit 角度**で、
  返す 0〜3 は 0°/90°/180°/270° を中心とする**象限**（14,15 が 0 へ巻き戻るのが証拠）。
- `UnitCursor.bcres` に**可視性アニメは 1 本も無い**（辞書スロット 11 が無い）。

したがってゲームの方式では任意の面積を出せない。**1 マスにつき 1 体**で作る。
12 体を毎フレーム動かして描いてもフレーム落ちは測れなかった（IDA-opus-5-F017、実機）。

**instance ヒープは体数から毎回計算する**（1 体 6,400 B + 16 KiB。実測は 1 体 5,090 B）。
固定 0x10000 だった頃は 12 体分しか無く、ソースの「16 体入る」は誤りだった（IDA-opus-5-F032）。
大きさを増やして入り切らないときは**解放して組み直す**。基点は握ったままなので位置はずれない。

ゲームに忠実な部分は残している。instance は `(0x834, 1, 3)` で作り、アニメ枠は
**slot 0 = 通常（常時）/ slot 1 = 置けないとき / slot 2 = 向き**というゲームの割り当てに合わせてある
（`ModelInstance_BindAnimSlot 0x004F0300`、IDA-opus-5-F029）。いまは slot 0 だけ使う。

### 18.2 実寸（確定。IDA-opus-5-F033）

| もの | 値 |
|---|---|
| 村の 1 マス | **world 32 単位** |
| `UnitCursor` の素寸（拡大率 100%） | **32 x 32**、厚み 0 |
| モデルの原点 | **quad の中心**（−16〜+16） |

実機での合わせ込み（間隔 32 / 拡大率 100）と、`UnitCursor.bcres` の SOBJ `0xDB0` の
バウンディングボックス（centre (0,0,0) / size (32,32,0)）が**独立に一致した**。
調べ直すときは `python tools/models3d/model_extent.py <bcres>`。

**原点が中心なので、丸め先はマスの中心**。`floor(p/32)*32 + 16`。
角へ乗せると必ず半マスずれる。

「マス目に合わせる」の丸めは **出した瞬間に 1 回だけ**。間隔を変えるたびに
丸め直すと `floor` の落ち先が変わってカーソルが飛ぶので、そうはしない。

別のモデルを使うときは、拡大率 = 間隔 / そのモデルの素寸 x 100。

### 18.2.1 軸の向き

実機で確かめた対応（IDA-opus-5-F032）:

| world | 画面 |
|---|---|
| `+X` | 下 |
| `-Z` | 右 |

だから画面の横 c ・縦 r は `x = 基点X + r*間隔` / `z = 基点Z - c*間隔`。
移動も並べ方もこの写像 1 つで決まるので、向きを直すときは `PoseAll` だけを見る。
**村のカメラの方位が固定であることが前提。** 屋内や別場面では確かめていない。

### 18.3 仕掛け

ゲームへ触るのは**描画スレッド上の 1 か所だけ**。メニュー側は要求を立てて状態を読むだけで、
確保・生成・解放も全部あちらで起きる。

| 物 | 場所 | 中身 |
|---|---|---|
| スタブ | `.text` の空き `0x00838700`（60 B・14 命令） | r0-r12/lr・CPSR・FPSCR・d0-d15 を保存し、最後の語の番地を呼んで戻す |
| フック | `0x001B721C` の NOP | スタブへの分岐。取り外しは `OnProcessExit` |

**★`0x00838600` は使えない**（gohan の field hook cave が既に居る）。空きは
`tools/gridcursor/verify_gridcursor.py` が `GuiCaves.h` と `FieldHookCaves.h` から毎回引き直して照合する。

毎フレーム 1 体につき 3 呼び出し:

```
Anim_SetFrame(anim, frame)                  0x004EDC98
ModelInstance_EvaluateAndApplyAnims(holder) 0x004F0560   ゲーム自身の適用（vtable[5]＋phase 0/1）
TransformNodeHolder_Submit(holder, 0)       0x004ED630
```

### 18.3.1 縞模様の向き（IDA-opus-5-F034）

`UnitCursor.bcres` の **`UnitCursorRotate`（0x161C、3 フレーム）** を **slot 2** へ結んで 1 フレームで止める。

| frame | 角度 |
|---|---|
| 0 | **45度** |
| 1 | 135度 |
| 2 | 225度 |
| 3 | 315度 |

メンバは `Materials["m_UnitCursor"].TextureCoordinators[1].Rotate` の 1 つだけなので、
**形も大きさも変わらない**（家の模様替えで見えるものと同じ）。
slot 0 の 150 フレームは `Translate` と `MaterialColor` しか書かないので衝突しない。

**★ OFF に戻すには組み直しが要る。** 枠を空にしても `Rotate` は最後の値のまま残る。

### 18.3.2 大きさを変えるときは必ず解放を挾む（IDA-opus-5-F034）

**`nwgfx_SkeletalModel_Create 0x0049693C` は確保の失敗を検査しない。**
instance ヒープが尽きるとゲーム自身が null を辿って落ちるので、戻り値や `node+0x1EC` の検査は手遅れ。

- 建てる前に `HeapGetFreeSize` で 1 体分（8,192 B）あるか見る
- 建てる数は **ヒープを作ったときの体数**。生の footprint を使わない
- 数の変更は **どの段からでも解放→組み直し**。Ready のときだけ見ていたのがクラッシュの原因

### 18.3.3 モデルビューア（IDA-opus-5-F035）

**パスの一覧は 3gx に持たない。** ゲームの RomFS を実行時に歩いて作る
（`RomfsIndex`）。埋め込むと 105〜253 KB になるところが 0 B で済む。
開き方は `ARCHIVE_ROMFS`（ID 3）＋ `PATH_EMPTY`で、タイトル ID もメディア種別も要らない。

- **歩き方**: 子と兵弟の鎖を辿る。スタックに積むのは**先祖だけ**。
  兵弟をまとめて積むと深さが**幅**になり、ルート直下で溢れてフォルダごと黙って落ちる
- **資源ヒープはファイルの大きさから**。.bcres は 16 KB 〜 5.5 MB、親ヒープの空きは 500 KB ほどしかない
- **モデル名は Setup の前に読む**。その段階のオフセットは確実に相対値
- メニューのリスト項目は **255 件が上限**（`optionCount` が u8）なので、200 件の窓をずらす

★**見える本数がいくつかで RomFS の見え方が決まる**: 1,907 = update だけ /
8,629 = base だけ / 10,430 = FS が重ねている。「一覧を作る」の通知にそのまま出る。

### 18.3.5 プレイヤー複製（IDA-opus-5.5-F028 / F029、試験・実機未確認）

目的（利用者 2026-09-24）: 髪型・髪色（将来は目・性別）の変更を、画面上の固定位置でプレビューする。
この版は**第 1 段: ワールドに出して、ゲームの手順で複製が作れ、髪だけ替わるかを確かめる**。画面固定・環境光なし・回転は後の段。
詳細は解析リポジトリの `docs/topics/player_clone_preview.md`。

- 作り方は人形 `AcNpcDemoDollPlayer` と同じ: `PlayerModel_ctor 0x1D3854` → `PlayerModel_CreateFromProfile 0x1CF090(pm, 本物のプロフィール, 0, マスク, 0, 4)` を 1 まで毎フレーム
- 部品はプレイヤー管理役（`*(0x94A374)`）のバンクの空き枠を使う。**作る前に全バンクの空き（+4 枠数 / +8 使用数）を確かめる**。空きが無いまま作るとゲームが 0 番地付近へ書いて落ちる
- 毎フレーム: 本物の体の行列を X 軸の向きへ 30 ずらして `PlayerModel_SetMatrix 0x1CEC10` → vtbl[2] → `PlayerModel_CalcAnim(pm,1,1)` → vtbl[4] → vtbl[5]（部品の更新と表への登録）→ `Scene_SubmitNode(体, 0)`
- 髪は複製の `pm+548`（髪型）/`+552`（髪色）だけ書く。次の部品更新 `PlayerModel_UpdateParts 0x1D0AEC` が読み直す。本物のプロフィール・本物のモデルには書かない
- 片付け: `PlayerModel_DestroyStep 0x1D30D0` を 1 まで → `PlayerModel_dtor 0x1D3980`
- **場面が変わったら、手放さずに枠を返す**（IDA-opus-5.5-F030。利用者報告: 出したまま建物に入ると無限ロード）:
  管理役 `BsPlayerMgr` は大域 `0x94A374` を 0 にしたあと、全バンクの使用数が 0 になるまで片付け（vtbl[20] `0x1C475C` → `sub_2A0E74`）を待つ。
  複製が枠を持ったまま手放すと永久に待つ。管理役・プレイヤー・部屋番号・場面の持ち主のどれかが変わったら、積むのをやめて 4 フレーム待ち、
  `PlayerModel_DestroyStep` と同じ順で、**作ったときの管理役**の `{バンク, 枠}` へ `BankTable_Release 0x2138FC` を返し終えるまで毎フレーム呼ぶ（大域は 0 なので使わない）
- **画面に固定**（IDA-opus-5.5-F031、試験）: `Render_DrawSceneIndexed 0x4EEDD8` の記録リスト再生直後の NOP `0x4EF1B0` に
  2 つ目のスタブ（`0x838780`、64 B、`tools/models3d/build_late_pass_stub.py` が生成・監査。R10 = 描いている Scene の文脈を R0 で渡す）。
  Scene 0 で、描画の文脈とカメラがゲームの大域の値と一致するときだけ、専用カメラを `CameraBind 0x494D3C` で結び、
  キャッシュ（文脈 +28 / +36）を消し、複製の体と部品の全メッシュを `Scene_DrawLayers` と同じ判定で層 0〜3 の順に `0x48D26C` で描き、元のカメラへ戻す
  （IDA-gpt-6-astra-F003 の木 1 体と同じ手順）。**複製は Scene 0 にも積む**（IDA-opus-5.5-F032）: 部品の骨格とスキニング行列は Scene の更新が
  計算するので、積まないとズボン（膝で曲がる）が止まった（利用者報告）。置く場所は (0, -5000, 0)（ゲームのカメラの far 1750 より遠く、世界では見えない）で、
  RotX(傾き) × RotY(向き)。部品は vtbl[5] が管理役の部品の表（生の管理役 +76768 から 32 おき 8 本、各 7 件 + 数 +28）へ積んだ分を読んで late pass でも描く。
  専用カメラ: view = 高さ 12・距離 120 から原点を見る。projection = ゲームのカメラの倍率に「大きさ」を掛け、w の行を足して中心を
  （画面の X, Y）へずらし、z の行に 0.01 を掛けて深度を near（50）のすぐ前へ寄せる（世界の物に隠れない）。
  Scene 1 へ積む案は、記録の格納先がフレームごとの 0/1（`dword_94CA54`）で Scene 間で共有されるため、右目が壊れるので採らない。
  **環境光はまだ世界のまま**（次の段）。Mii マスクの頭は Mii 専用の描画へ回るので出ない
- **使用中ビットと使用数だけを消してはいけない**: `BankTable_Release` は vt[0]（頭なら `0x822B98`）で部品を下ろしてから空きにする。
  下ろさずに空けると、管理役の片付けがテクスチャアニメを先に消したあとで頭を下ろし、アロケータ 0 で落ちる（実機 SIGSEGV `0x569B48`）

### 18.3.4 公共事業（IDA-opus-5.5-F001 / F002 / F004 / F005）

- 建物表は `garden + 0x4BE80`（`[0x955F8C]`）。+4 が数、+8 から `{u16 id, u8 x, u8 y}` × 56
- 置くのはゲームの関数。消す・動かすは表の書き換えで、**そのあと必ず**
  `Field_RebuildAttributes 0x242878`（区画を素に戻して全部押し直す）と
  `Building_RebuildOccupancy 0x24220C`（占有を全部書き直す）を呼ぶ。呼ばないと再起動まで古い場所に当たり判定が残る
- 置く・消す・動かすは**全建物**（役場・店・家も。利用者指示 2026-09-23）。リストは名前のある 175 件
- マイデザインの看板・顔出し看板（0xDC/0xDD）は Vapecord と同じく看板表 garden+0x4BF70（0x878 B × 8）も書き換える
- 実機確認済み（T004）: 消す・動かすと当たり判定はその場で更新され、アイテムは消えない
- **見た目は読み直さずに作る・消す**（F004、実機未確認）。村の読み込みが 1 件ずつやっている手順
  （`0x6DE198`: 位置 → 高さ `0x6C69C0` → 橋なら +32 → `Actor_SpawnByProfile 0x57ADFC` → 管理役 `[0x94A6C0]` の一覧へ）を写す。
  消すときは**一覧から抜いてから** `0x5200B8` で削除要求（AcStrc の dtor は一覧から外さず、管理役は毎フレーム一覧を読む）。
  特別扱いの id（80〜83 役場、89、90 踏切、93 船）、村で作らない id（dobj_* 商店街など）、実体が見つからないときは部屋を読み直す。
  通知の「その場で反映」「部屋を読み直しました」でどちらだったか分かる
- F004 は**実機確認済み**（その場で出る・消える・高さ一致・落ちない）
- **足元のアイテム**（F005、実機未確認）: 足元 16×16 マスに `Field_MarkDirty 0x59DA7C(x, y, 1)`（ゲームがアイテムを消した後に呼ぶもの）。
  マスの物体を壊して「未構築」に戻し、今の地面で作り直させる。座標は 0〜0x6F / 0〜0x5F に絞る（境界を見ない関数がある）
- **下画面の地図**（F005、実機未確認）: BsMenuMapVillage を基本マネージャ `[0x96F2A4]` のスロットから vtable で探し、
  建物アイコンの 3 プールを隠して数を 0 に戻し、作成段 9 と同じ `sub_221190(map, 0, 1)` を呼び直す
- F005 は**実機確認済み**
- **地図の枠あふれ**（F006）: ゲームの割り当て `0x222434` は数を見ない。A 枠（役場・店など、10）・B 枠（橋など、8）を
  溢れさせると星アイコン・線路の柵の消滅・クラッシュ（ゲーム自身の読み直しでも起きる）。id→枠の表
  `Includes/Cheats/MapIconPools.h` は `tools/strc/map_icon_pools.py` が割り当て関数を Unicorn で走らせて作る（手で編集しない）。
  **置く数は地図の枠で制限しない**（利用者指示）。**ゲームのコード・上限・読み込み処理は書き換えない**（利用者指示 2026-09-23）。
  その場の地図更新は、画面遷移のときと同じ作成段 9 の関数を作りたてと同じ状態で呼ぶ。枠に収まるときだけ。
  超えたら何もせず画面切り替えに任せる（星アイコン等は素のゲームのまま）
- **資源は 2 系統**（F007 / F008）。公共事業・橋など 7 クラスは共有の 64 枠（1 種類 2 枠、部屋が片付くまで返らない）、
  役場・店などは実体ごとの専用ヒープを親 `*(0x94CC68)` から借りる。その場で作るのは、共有 7 クラスなら空き 3 枠・ヒープ 0x30000 B 以上、
  それ以外は親ヒープの空き 0x40000 B 以上のとき。足りなければ部屋を読み直す
- **共有 7 クラスの種類数が 30 を超える設置は断る**（`TooManyKinds`）。64 枠を超えるとゲーム自身の部屋の読み込みが落ちる
- **役場・店などで親ヒープの空きが 256 KB 未満なら断る**（`NoHeapRoom`、F009）。読み直しても借りる量は同じなので逃げ道にならない
- **地図**（F009）: A ≤ 18 かつ B + 1 ≤ 8 なら作成段 9 の関数を呼ぶ（A が 10 を超えた分はゲームと同じく星アイコン）。
  A 添字 18 / B 添字 8 は初期化されていないアイコン（+0x6284）でゲームでも落ちるので、そこへ届く設置は断る（`MapLimit`）
- **プレイヤーの位置は変えない**（利用者指示 2026-09-23）。読み直しへ退避するときも今の位置
- ゲームの関数は全部描画スレッド（グリッドカーソルのフックの相乗り枠）で呼ぶ
- 検査: `python tools/strc/verify_publicworks.py`

### 18.4 ゲーム関数を C++ から呼ぶときの落とし穴（実際に踏んだ）

**`float` を取るゲーム関数でも、s0 で受けるものとスタックで受けるものがある。**
devkitARM は `-mfloat-abi=hard` なので `float` と宣言すれば s0 に載る。
ゲーム側がスタックから読む関数だと、スタックの語が書かれずゴミが渡る（IDA-opus-5-F031）。

| 関数 | ゲームの読み方 | 宣言 |
|---|---|---|
| `Heap_CreateNamed 0x0031785C` | `vldr s16,[sp,#44]` = **呼び出し元 SP+4** | **`u32` で 0 を渡す**。`float` は不可 |
| `Anim_SetFrame 0x004EDC98` | `vmov.f32 s16, s0` | `float` でよい |
| `Anim_SetRate 0x004EDC74` | `vmov.f32 s16, s0` | `float` でよい |

**関数ごとに逆アセンブルで確かめる。** 型だけ見て決めない。

### 18.5 「失敗ではない停止」を黙らせない

検査が `Stage::Failed` にしない停止（段が進まない、シーン所有者が合わない）は通知が出ない。
これで一度、**スタブは毎フレーム走っているのに `StepBuild` が一度も呼ばれていない**状態を
実機のメモリを読むまで気付けなかった（IDA-opus-5-F031）。「状態を見る」はそのために足した。

**★所有者の一致検査は、所有者を控えたあとにしかかけてはいけない。**
控えるのは `AllocHeaps` の中なので、その前にかけると永久に進まない。

### 18.4 安全側の作り

- **部屋が変わったら自分で止まる。** シーン所有者 `[0x00948E70]` とその資源を生成時と比べる。
  読み込み中は 0 になり、戻っても**別番地で作り直される**ので以後ずっと描かない（IDA-opus-5-F024）。
  自前 instance は場面転換で**一切触られない**ことを実測済み。
- **生成の検査は戻り値だけでは足りない。** `node+0x1EC` が 0 ならヒープ不足で半分しかできておらず、
  描くと落ちる（V2-F025）。
- **姿勢は `UpdateWorldAndSkeleton 0x004ECFEC` まで。** 行列を書くだけでは骨の配列が埋まらず、
  submit されても 1 体も描かれない（IDA-opus-5-F020）。
- **解放の順序**: instance → アニメ（**vtable スロット 5**。デストラクタでは解放されない）→
  資源ホルダ → ヒープ。**資源ホルダはヒープ破棄より前**（逆にすると SIGILL。IDA-opus-5-F027）。
- **入れる前に空きを確かめる。** 置き場が全 0 でない、フック先が期待の NOP でない、のどちらでも
  **何も書かずに断る**（通知に理由を出す）。

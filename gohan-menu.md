# gohan チートメニュー項目の挙動仕様

自前チートメニュー（`GuiMenu`）の項目が、どの状態を持ち、何によって変わるかを定める。
機能の一覧は [gohan.md](gohan.md)。ここは**振る舞いの側**だけを扱う。

> **記述の種別**
> | 印 | 意味 |
> |---|---|
> | （無印） | 現行実装の契約。`Includes/Gui/GuiMenu.hpp` と `Sources/Gui/GuiMenuModel.cpp` に基づく事実 |
> | **【確定】** | 利用者が定めた必須条件。**案ではない。実装はこれに従う** |
> | **【解釈】** | こちらの読み。**利用者の確認待ち**。違えば直す |
> | **【CLAUDE案】** | こちらからの提案。採否は未定 |

---

## 1. ★用語 — 2 つの ON/OFF を分ける

本書および今後の仕様書では、次の 2 つを**常に別物として書く**。混同しない。

| 用語 | 実体 | 誰が変えるか |
|---|---|---|
| **項目の ON/OFF**（＝**適用値** / アーム状態） | チェック項目の `applied`（0 または 1） | **メニュー操作**（関数側からも可能だが非推奨。[§5.5](#55-トグル型アクション式確定) の例外を除く） |
| **効果の ON/OFF**（＝**関数内の ON/OFF**） | `ToggleEffectFuncs::IsActive(index)` が返す値。原則として**ゲーム側メモリを読んで判定する**（「効果が書かれているか」） | ホットキー押下、または関数側 |

**この 2 つは独立している。**

- **ホットキーを押しても項目の ON/OFF は変わらない。** 変わるのは効果の ON/OFF だけ。
- **アーム**（本書の用語）＝ **項目を ON にしても効果は起こさず、ホットキーを受け付ける状態にするだけ**、という状態。
  銃の安全装置を外す arm から。[§4.3](#43-パッチ型--複数アドレスへのパッチ確定) の「ホットキー束縛あり」がこれに当たる。
- 項目の ON/OFF は**基本的にメニューから利用者が行う**。そうでないと挙動が自明でなくなる。
- 関数側から項目の ON/OFF を変えること自体はできる（`SetItemApplied`）。**非推奨**。
  正当な例外は [§5.5](#55-トグル型アクション式確定) のトグル型アクション式だけ。

### ★編集 → 適用待ち → 適用

**値・ON/OFF・ホットキー設定の変更は同じ 3 段を通る。** アクション式の実行は例外で、選択した時点で実行する（[§5.1](#51-アクション式確定)）。アクション式のホットキー設定を変更する場合は、通常どおり適用を経由する。

| 段 | 状態 | 操作 |
|---|---|---|
| **編集** | `value` が変わる | 項目を操作する（チェックを反転、数値を入れる、リストで選ぶ、束縛を変える） |
| **適用待ち** | `value != applied` または `hotkey != appliedHotkey` | **文字が黄色**（`#ffd166`）になる。**まだ何も起きていない** |
| **適用** | `value` → `applied` | **X ボタン**（選択中の項目）／**メニューを閉じる時の確認ダイアログ**（全項目一括）。L ボタンで差し戻せる |

- **設定変更の結果が反映されるのは「適用」した時。** 編集しただけでは反映しない。即時アクションの実行とは区別する。
- **ホットキー押下は項目ごとの振る舞いに従う。** 効果の反転、単発実行、入力UIの表示などがあり、トグル型アクション式は確認ダイアログを経由する。
- ★**「適用」と「書き込み」を混同しない。**
  「適用」はメニュー上の操作であり、「アドレスへ書き込む」「変数へ代入する」はその結果として起きる処理の一つにすぎない。

| | 意味 |
|---|---|
| **編集値** `value` | 操作中の値。適用していない |
| **適用値** `applied` | 適用済みの値。**関数側が読むのはこちら** |

ホットキーの束縛も `hotkey`（編集中）と `appliedHotkey`（適用済み）に分かれる。
関数側が参照するのは `ItemApplied()` と `ItemAppliedHotkey()`。

---

## 2. 状態遷移の規則

### 通常チェック項目（トグル式）のホットキー押下

| 条件 | 起きること |
|---|---|
| メニューを開いている | **発火自体が走らない** |
| 項目が OFF（`applied == 0`） | **何もしない** |
| 項目が ON（`applied != 0`） | 項目の振る舞いに従う（[§4](#4-項目の挙動例確定)） |

**いずれの場合も項目の `applied` は変化しない。**

項目のON/OFFを持たないアクション式・値入力等は [§3](#3-項目種別ごとの挙動)、トグル型アクション式は [§5.5](#55-トグル型アクション式確定) と [§7.4](#74-ib_apply_once-とは) に従う。

### メニュー操作

| 操作 | 起きること |
|---|---|
| 項目を編集する | `value` が変わり、**適用待ち（黄色）**になる。**効果は起きない** |
| **適用する**（X／閉じる時の一括） | `value` → `applied`。**ここで効果が起きる**。型ごとの挙動は [§3](#3-項目種別ごとの挙動) / [§4](#4-項目の挙動例確定) |
| 差し戻す（L） | `value` を `applied` に戻す。効果は起きない |

---

## 3. 項目種別ごとの挙動

| 種別 | 項目の ON/OFF | 操作（A） | **適用**（X） | ホットキー |
|---|---|---|---|---|
| **チェック項目**（トグル式） | **ある** | ON/OFF が反転 | [§4](#4-項目の挙動例確定) の型による | [§4](#4-項目の挙動例確定) の型による |
| **アクション式** | **無い** | **1 回実行**（適用を経由しない） | — | **無条件に 1 回実行** |
| **数値入力式** | **無い**（値そのものが設定） | 数値入力を開く | 書き込み／代入（[§5.2](#52-数値入力式確定)） | 数値入力を開く |
| **スライダー式** | **無い** | スライダーを開く | 同上 | スライダーを開く |
| **リストボックス式** | **無い** | その場にリストを展開 | 同上（[§5.3](#53-リストボックス式確定)） | その場にリストを展開 |
| **連動型**（数値・リスト） | **無い** | 上の数値入力式／リストボックス式と同じ | ゲームへ書き込む。**値の固定**（設定画面から ON）なら以後も値を保つ（[§5.6](#56-値の固定連動型実装済み実機未確認)） | 上と同じ |
| **トグル型アクション式** | **ある（メニュー実行の適用待ち用）** | ON/OFF が反転 | **束縛の有無を問わず即実行し、項目をOFFへ戻す** | **確認ダイアログを表示し、承諾後に1回実行。ON/アームは不要** |
| **フォルダ** | — | 中へ入る | — | — |

---

## 4. 項目の挙動例【確定】

**以下 3 件は利用者が定めた必須条件。実装はこれに従う。**

### 4.1 押下中実行型 — 押している間だけ

> 項目が ON の時、**ホットキーを押している間のみ実行する**。関数内定義の ON/OFF は**無い**。

- 項目 OFF のときは何もしない。
- ホットキーは**修飾キー**であって、トグルの引き金ではない。**押下で何も反転しない。**
- メニューを開いている間は自動的に止まる（押下の水準が 0 になるため）。
  C スティック・スライドパッドを直接読む場合だけ `IsVisible()` で自前に止める。

該当: `座標移動`

### 4.2 単発実行型 — 押すたびに同じ処理を 1 回

> 項目が ON の時、**ホットキーが押されるたび同じ処理を一回行う**。
> 関数内定義の ON/OFF 機能は**無く、必ず同じことを行う**。

- 項目 OFF のときは何もしない。
- 内部状態を持たない。**何度押しても毎回同じ動作**。

該当: `アクション解除`
（プレイヤーの動作状態を **Idle（state `0x06`）へ強制移行**させる機能。
旧 F-66 で `player+0x1A9` が現在状態、状態 6 が neutral/wait であることを確認している）

### 4.3 パッチ型 — 複数アドレスへのパッチ【確定】

> - 項目が ON になると**複数のアドレスに書き込みを行う**。**OFF になると解除する**。
> - ホットキーを入力すると、関数内定義による ON/OFF でそれを実行する。
> - 内部にフラグを持ち、**項目が ON の時にフラグを立てる**。
>   つまりホットキー入力では、**最初にそのフラグが取り下げられる**順序になる。
> - **ホットキーが設定されている状態で項目を ON にした場合、その時点ではフラグは立たない。**
>   つまり次のホットキー入力で、**最初にそのフラグが立つ**順序になる。

規則としてまとめると次のようになる。

| | 項目 ON を**適用**した時 | ホットキー押下 | 項目 OFF を**適用**した時 |
|---|---|---|---|
| **ホットキー束縛なし** | パッチを**貼る**（フラグが立つ） | — | **剥がす** |
| **ホットキー束縛あり** | **アームのみ**（フラグは立たない） | 貼る／剥がすを**反転** | **剥がす** |

※ 編集して黄色になっただけでは何も起きない（[§1](#編集--適用待ち--適用)）。

→ **駆動元は、ホットキーが束縛されているかで決まる。**
束縛が無ければメニューが駆動し、束縛があればホットキーが駆動する。

★**項目 OFF では、ホットキーで貼った状態であっても必ず剥がす。**
パッチが当たったまま項目が OFF に見える状態は原因を追えなくなるため（2026-09-13 承認）。
これは「メニューを OFF にしても効果には触らない」という現行契約からの**変更**である。

**ホットキー設定だけの変更【確定・2026-09-14】**：項目のON/OFFを変えずにホットキーを追加・変更・解除して適用しても、現在の効果のON/OFFは変わらない。パッチを貼り直す・剥がす・反転する処理は行わない。束縛解除を理由に効果をONへ揃えない。上の駆動規則は項目のON/OFFを適用する際の規則である。

該当: `花散らせない`、`穴に落下しない`、`壁抜け`、`店24時間オープン` ほか
**複数アドレスへのパッチだけで済むものは、この型を既定とする。**

---

## 5. 種別ごとの挙動

§4 と同じ書き方で示す。**【確定】は利用者が承認済み、【解釈】は確認待ち。**

### 5.1 アクション式【確定】

> **項目の ON/OFF を持たない。アームの概念も持たない。**
> 選ぶとその場で 1 回実行する（**適用を経由しない**）。
> **ホットキーが設定されると、そのホットキーを押すたびに必ず 1 回発火する。**
> 関数内 ON/OFF は無い。

**[§4.2](#42-単発実行型--押すたびに同じ処理を-1-回)（単発実行型）との違いは、発火に条件が付くかどうかだけ。**

| | 発火の条件 |
|---|---|
| **アクション式** | **無条件**。ホットキーが入力されれば必ず発火する |
| **単発実行型**（トグル式＋1 回実行） | **項目が ON のときだけ**。OFF なら押しても発火しない |

該当する動作が誤発して困るなら、アクション式ではなく [§4.2](#42-単発実行型--押すたびに同じ処理を-1-回) の型を選ぶ。

該当: `ポケットアイテム`、`スタイルを変更`、`選択した住民への操作`、`選択中アイコンに部屋移動`（`公共事業エディター` は 2026-09-24 に実装しトグル式にした）

### 5.2 数値入力式【確定】

> **項目の ON/OFF を持たない。値そのものが設定である。**
> 編集すると適用待ち（黄色）になる。**適用すると、その値を書き込む／代入する。**
> ホットキーが束縛されていれば、数値入力を直接開く。

**適用先は 2 通りある。**

| 適用先 | 内容 | 例 |
|---|---|---|
| **アドレスへ書き込む** | ゲーム側のメモリへ値を書く。連動型（[§5.4](#54-連動型確定)）はこちら | `所持金`、`カブ` |
| **変数へ代入する** | プラグイン内の変数へ入れる。**参照する側のチートがそれを読む** | `座標移動 -> 移動量` |

- `座標移動 -> 移動量` は変数へ代入し、`座標移動` がその変数を毎フレーム読む。
- `ドロップアイテム` も変数側。読む「ドロップ系チート」は未定。

該当: `座標移動 -> 移動量`、`ドロップアイテム`

### 5.3 リストボックス式【確定】

> **項目の ON/OFF を持たない。** 数値入力式（[§5.2](#52-数値入力式確定)）と同じく、
> **適用した時に書き込む／代入する。** 適用先が 2 通りあるのも同じ。

| 適用先 | 挙動 | 例 |
|---|---|---|
| **変数へ代入** | 参照する側のチートが読む | `座標移動 -> 移動キー`、`座標移動 -> 移動方法` |
| **アドレスへ書き込む** | 連動型（[§5.4](#54-連動型確定)） | `天気` ほか |

### 5.4 連動型【確定】

> **項目の ON/OFF を持たない。**
> **メニューを開いたその時に、ゲーム側の現在値を 1 回読み取る**（開いている間ずっと同期はしない）。
> 読み取った値は編集値と適用値の両方に入る（＝開いた直後は適用待ちにならない）。
> **適用した時にゲームのアドレスへ書き込む。**

- 書き込み後に再構築が要るもの（見た目など）は、**書き込みの側が再構築まで面倒を見る**。
  値だけ書いて画面が変わらない、という状態を利用者に見せない。
- **読み取りに失敗したとき**（セーブ未ロードなど）は、**その項目を編集できなくする**。カーソルは置ける（説明を読める）が、値の変更・決定・ホットキー設定はできない（2026-09-17 利用者指示。Simulator との意図した差）。
  誤った値を見せない・書かせないため。**この挙動は変更可能**。

該当: `所持金`、`貯金`、`ふるさとチケット`、`メダル`、`カブ`、`天気` ほか。見た目の各設定は独立した連動型項目ではなく、`スタイルを変更`の専用メニューへまとめる（[gohan.md §5](gohan.md#5-rootplayerplayerstyles)）。

### 5.5 トグル型アクション式【確定】

利用者の定義:

> チートメニューから ON/OFF が可能で、**ON にすると必ず適用待ち状態になる**。
> **適用されると OFF に戻り、アクションが実行される。**

こちらの読み（**利用者の訂正を反映済み**）:

> - 項目を ON にすると、ほかの項目と同じく**適用待ち（黄色）**になる。この時点では何も起きない。
> - **適用した時にアクションを 1 回実行し、同時に項目を OFF へ戻す。**
>   ゲーム側の条件成立を待つのではない。**適用が引き金**。
> - 項目を OFF に戻して適用すれば、実行せずに取り消せる。

- ★**実行後に項目を OFF へ戻すが、数フレームの間は「実行された」ことを明確に表示する。**
  一瞬で OFF に戻ると、押したのに何も起きていないように見えるため。

**「実行した」表示【確定】**

| 対象 | 実行後の表示 |
|---|---|
| **値欄**（通常は `ON` / `OFF`） | **`OK` など**に差し替える |
| **文字色** | **値欄（`OK` の部分）だけ**を赤または黄色にする |
| **通知** | 同様に出す |

現行実装（2026-09-17、Simulator `0cabaed` を移植）: 値欄を `OK` に 800ms 差し替え、`ACTION` / `〈項目名〉を実行しました` を通知する。
Simulator が「通常アクションと同じ見た目」に統一したため、`OK` の色は行の色（選択中は白）で、値欄だけの別色は付けていない。
- ★**これが「関数側から項目の ON/OFF を変える」唯一の正当な例**になる（[§1](#1-用語--2-つの-onoff-を分ける)）。
- 現状の `しずえスキップ` はこれとは別の形（部屋 `0x63` に入るまで毎フレーム待ち、実行後も項目は ON のまま）。

該当: `気絶`

**メニューとホットキーの関係【確定・2026-09-14最終訂正】**：ホットキー付きでもメニューのON→適用で即実行し、OFFへ戻る。ホットキーでは確認ダイアログを表示し、承諾後に1回実行する。取消では実行しない。ホットキーの発動に持続的な項目ON/アームは要求しない。設定した束縛は適用後に使い、束縛設定だけの適用では実行しない。

**アクション式との差は2点だけ**：メニュー実行で適用待ちを経由すること、ホットキー実行で確認ダイアログを表示すること。通常トグル式のON/アーム条件をこの型へ流用しない。

### 5.6 値の固定（連動型）【実装済み・実機未確認】

2026-09-17 に設計し、2026-09-25 に **Simulator 670e44f（`issue-fixes.js`）の形**で実装した（gohan `work/sim-0925`）。

> 連動型数値入力／連動型リストボックスで、**数値やリストの選択項目をその値に常に固定する。**
> ゲームが値を変えても（買い物でお金が減る、天気が変わる など）、固定した値へ戻し続ける。

**対象**: 連動型数値入力（`ITEM_LINKED_VALUE`）と連動型リストボックス（`ITEM_LINKED_LIST`）だけ。

**操作（Simulator の形。2026-09-17 の設計案「R で切り替え・適用待ちを経る」から変わった）**

| 操作 | 起きること |
|---|---|
| 対象の項目を選んだまま **START** → 設定画面の **`値を固定`** で A | **その場で**切り替わる（適用待ちを経ない）。ON にすると編集値を適用値にして 1 回書き、通知 `値を固定` / `〈項目名〉: ON` |
| 同じ操作をもう一度 | OFF。通知 `〈項目名〉: OFF`。ゲーム側の値は最後に書いた値のまま |
| 固定中に値を編集して適用（X・閉じる時の一括・ホットキー） | 新しい適用値で固定し続ける |
| 読み取りに失敗している項目 | 新たに固定できない（`値を固定` が灰色）。固定中なら OFF にはできる |

**駆動（固定 ON の間。メニューの開閉に関係なく、メニュースレッドの周期 16ms）**

- `LinkedRead` で読めて、かつ固定値と違うときだけ `LinkedWrite(固定値)` を呼ぶ。同じ値を毎フレーム書かない。
- 読めないフレームは何も書かない（セーブ未ロード・場面転換中）。固定は解除しない。
- メニューを開いたときの読み取りでは固定値を上書きしない（固定した値を表示し続ける）。

**表示**: 値欄を水色（`#5cc8ff`）。項目の色は変えない。説明欄の操作の欄に `値を固定:ON`。
（Simulator 639b4e6 から、値の固定は行の左端の縦線を使わない。縦線は §5.7 の状態の印だけ）

**未定**: 固定に向かない項目を禁止する仕組み（案 `SetLinkedLockable(index, false)`）はまだ無い。`Write` は「その値にする」処理である前提（呼ぶたびに副作用が積み重ならないこと）。

### 5.7 設定画面・お気に入り・保持【実装済み・実機未確認】

Simulator 670e44f（`issue-fixes.js` / `issue-overlay.js`）を移植した（2026-09-25）。
**Simulator 639b4e6**（`app.js` / `issue-fixes.js` / `issue-overlay.js` / `docs/menu-state-indicators.md`）に追従した（2026-09-26。実機未確認）。

- **START** でメニュー内に設定画面（`SETTINGS`）を開閉する。ダイアログ・入力 UI・インラインリストが出ている間は効かない。
  設定画面では X・Y・L・R・SELECT・左右を受け付けない。**★gohan の意図した差**: 受け付けないのはメニューが開いている間だけ
  （Simulator はメニューを閉じても設定画面が残っていると受け付けないので、閉じた後のホットキーが効かなくなる）。
- 設定画面の項目: `FAVORITES`（お気に入り一覧を開く。1 件も無ければ灰色）、`値を固定`（§5.6）、
  `この項目を保持`（選んでいた項目の適用済みの状態だけを保持。全体の設定とは独立）、`値の固定を保持`（既定 OFF）、
  `お気に入りを保持`（既定 ON）、`オンにした項目を保持`（既定 OFF。値・リスト等も含む。値の固定は含まない）、`オンにしたお気に入りを保持`（既定 OFF）。
  設定画面の A は決定して保存も頼む。
  **★gohan の意図した差**: `この項目を保持` は連動型では灰色（固定していない連動型は保持しない、という下の決定のため。
  連動型を次回も同じ値にしたいときは `値を固定` と `値の固定を保持`）。
- **R** で選んだ項目をお気に入りに登録／解除（通知 `FAVORITES` / `ADD: 〈項目名〉`・`REMOVE: 〈項目名〉`）。
  一覧は元の項目そのものを並べるので、ON/OFF・値・適用待ち・ホットキーは共有される。一覧の中で最後の 1 件を外すと一覧を閉じる。
- **行の状態の印**（Simulator 639b4e6。`F` / `H` の字は廃止）: 行の左端（menuX+4、行の y−2）に幅 1px・高さ 12px の縦線を、
  上から お気に入り（`#d6c98a`）・この項目を保持（`#e5484d`）・ホットキー（`#78a9ff`）の順に、立っているものだけで等分して描く
  （i 番目 = [floor(12·i/n), floor(12·(i+1)/n))）。不透明度 60%。選択中の行は決定の弾みと一緒に動く。値の固定は線を足さない。
- **フッター**（字ごとに位置を固定。変更数が 2 桁でもずれない）: 1 行目 `A決定`(+6) `X適用`(+30) `Yホットキー`(+54) `START設定`(+102)、
  2 行目 `n変更`(+6) `B戻る`(+54) `L戻し`(+102) `R星`（`START設定` の右端から空白 1 つ）。
- **説明欄**: キーの案内（`R:FAV` など）は出さない。値の固定中だけ `値を固定:ON`。無効な項目の説明も読める色（`#c4cec7`）のまま。
- **保持**（`GohanCTRPFData.bin` の gohan の欄。CTRPF の通常メニューの設定と同じファイル。**旧形式からの移し替えはしない**）:
  お気に入り（`お気に入りを保持` が ON のとき）、保持の設定、適用済みの値（`オンにした項目を保持` なら全項目、
  `オンにしたお気に入りを保持` だけならお気に入りだけ）。対象はチェック・数値・スライダー・リスト。編集中の値は入れない。
  `この項目を保持` の印を付けた項目の適用値（常に。全体の保持の後に戻す）。値の固定（`値の固定を保持` が ON のときだけ）。
  鍵は項目の階層パス（ラベルを `/` で連結）。
  - 保存: 自前メニューを閉じ切ったとき（中身が変わっていれば）と終了時。CTRPF の通常メニューを閉じたときの保存でも書かれる。
  - 復元: `自前 GUI を組み込む` で自前メニューを起動したとき（各チートの登録の後）。チェック項目の効果は適用のときと同じ規則
    （ON かつホットキーの束縛なしのときだけ効果 ON）。連動型（固定していないもの）は**ゲームが読めるようになった最初のフレームで書く**。
  - ★**固定していない連動型は保持しない**（利用者の決定 2026-09-25。Simulator との意図した差）。次の起動で前回の値へ書き戻さない
    （所持金などが勝手に戻らないように）。固定は `値の固定を保持` が ON なら固定として戻り、ゲームが読めるようになってから書く。
    2026-09-26 より前の保存（固定を `オンにした項目を保持` の欄に入れていた）の固定も読む。
  - CTRPF のメニューを閉じたときの保存が自前 GUI の起動より先に走っても、ファイルにある gohan の欄はそのまま残す。
  - ★**ホットキーは常に保持する**（利用者の指摘 2026-09-26。Simulator は保存しない。CTRPF の通常メニューと同じ扱い）。
    保持の設定に関係なく、既定と違う適用済みのホットキーを項目の階層パスで保存し、起動時に戻す（戻したあと効果の規則を取り直す）。
    gohan の欄の末尾に足した欄なので、それ以前の保存もそのまま読める（ホットキーは既定のまま）。
- **長押し（X / L）**: 2/5（240ms）未満で離すと単体の適用／戻し、2/5 以上・満了前で離すと何もせず取り消し（通知 `HOLD` /
  `全適用をキャンセルしました`・`全戻しをキャンセルしました`）、満了で確認ダイアログ。2/5 未満の間は進捗と枠を灰色。
- **無効（灰色）の項目**は、一括適用でも適用しない（カーソルは置ける）。
- **通知**は件数でなく高さで押し上げ、画面の上へ出たものから消える。題は幅 - 20、本文は幅 - 14 で折り返し、1 行ごとに 10px 伸びる。

---

## 6. 関数側の連携（現行）

用途に応じて選ぶ。いずれも**メニュースレッドから呼ばれる**ので短く書く。項目番号は `GuiMenu::FindItem("項目名")` で引く。
宣言は `Includes/Gui/GuiMenu.hpp`。

### (a) `RegisterToggleEffect` — チェック項目の効果の ON/OFF

```cpp
struct ToggleEffectFuncs {
    bool (*IsActive)(int index);            // 効果が書かれているか（読んで判定する）
    void (*SetActive)(int index, bool on);  // 有効値／既定値を書く
};
GuiMenu::RegisterToggleEffect(index, &funcs);
```

- [§4.3](#43-パッチ型--複数アドレスへのパッチ確定) の駆動規則は**エンジンに組み込み済み**（項目 ON の適用で束縛なしなら ON／束縛ありならアームのみ、OFF の適用で必ず OFF、ホットキーで反転、ホットキー設定だけの適用では触れない）。
- **既定値は必ず明示値にする。初回書込値の自動保持はしない。**
- **★効果が既定で ON のときは、登録直後に `GuiMenu::SetItemApplied(index, 1)` を呼ぶ。**
  （2026-09-23 追記）**この規則は当初効いていなかった。** 項目を読み書きする関数はすべて `ItemOk()` を通るが、
  それが「メニューのスレッドが動いているか」で判定していて、登録（`WireBehaviors`）は**スレッドを作る前**に
  走る。このため登録時の `SetItemApplied` / `SetItemOptions` / `ItemApplied` は**黙って何もしなかった**
  （公共事業のリストが「読み込み中」のまま、利用者報告）。`ItemOk()` を「項目数の範囲内か」だけにした。
  チェックの見た目は**項目の `value`** が持っていて、`RegisterToggleEffect` が呼ぶ `PrimeEffect` は
  通知用の `g_effectSeen` しか更新しない。揃えないと**表示だけ OFF で振る舞いは ON** になり、
  一度 ON にして OFF に戻すまで実態と合わない（2026-09-22 利用者報告。`root/テスト` の
  「マス目に合わせる」で発生）。`IsActive` が登録時点で偽ならこの一行は要らない。
- メモリを書いたら `GuiMenu::FlushMemory(addr, size)` を呼ぶ。番地の並びで済むものは `TogglePatch` ＋ `PatchListIsActive` / `PatchListSetActive`。
- **通知は効果（関数内定義）の ON/OFF から出す**。題は項目名、本文は `〈項目名〉を有効にしました`（緑）／`〈項目名〉を無効にしました`（赤）（2026-09-17 利用者指示。`CHEAT ENABLED {番号}` の形式は使わない）。エンジンが毎フレーム `IsActive` を観測し、前回と違うときだけ通知する。
  効果は項目 ON の適用（束縛なし）・OFF の適用（必ず解除）・ホットキーの反転・関数側の書き込みのどれでも変わるので、原因を問わず変化で出る。束縛ありの ON 適用（アームのみ）や、効果が既に同じ状態のときは出ない。
  **メニューで項目を有効/無効にしただけでは通知しない**（効果を登録していない `しずえスキップ` やテスト用チェック項目も含む）。登録した瞬間の状態は通知しない（2026-09-17 利用者指示。Simulator との意図した差）。

### (b) `RegisterLinked` — 連動型（[§5.4](#54-連動型確定)）

`Read(index, &value)` をメニューを開いた瞬間に 1 回呼ぶ。false なら項目を編集不可にする（カーソルは置ける）。適用で値が変わったら `Write(index, value)`。

### (c) `RegisterExecute` — 実行

アクション項目は決定／ホットキーで、トグル型アクションは ON の適用／ホットキー確認の承諾で 1 回呼ぶ。

### (d) `RegisterApply` — 数値・スライダー・リストの適用

値が変わった適用のときだけ `Apply(index, value)` を呼ぶ。「変数へ代入」型は登録せず `ItemApplied()` を読むだけでもよい。

### (f) `RegisterDisabled` — 無効状態（2026-09-18 追加）

`RegisterDisabled(index, IsDisabled)` を登録した項目は、メニューのスレッドが毎フレーム `IsDisabled(index)` を呼んで無効状態（`disabled`）を決める。
無効な項目はカーソルを置けるが、値の変更・決定・ホットキー設定はできない（[§5.4](#54-連動型確定) の読み取り失敗と同じ扱い）。
例: `座標移動 -> 移動量` は `移動方法` の編集中の値がグリッド単位の間は無効。

### (e) `SetToggleHandlers` — 毎フレーム動かす

`OnEnable`（有効化の 1 回）/ `OnTick(index, held)`（有効中毎フレーム）/ `OnDisable`（無効化の 1 回）。
`held` はメニュー・入力 UI・確認ダイアログの表示中は 0。

旧 `SetHotkeyHandler`（全項目で 1 本）と `SetEffectMenuJudgment` は 2026-09-17 に廃止した（§4.3 の規則が既定になったため）。

---

## 7. 【CLAUDE案】項目ごとの振る舞い記述子

### 7.1 現行設計の限界

効果は項目ごと（`g_fx[]` / `g_fxMenuJudge[]`）なのに、**コールバックはグローバル 1 本**という非対称がある。

```cpp
void SetHotkeyHandler(HotkeyHandler handler);      // 全項目で 1 本
void SetToggleHandlers(const ToggleHandlers *h);   // 全項目で 1 本
```

この結果、§4 の 3 型のうち

| 型 | 現行での可否 |
|---|---|
| §4.1 押下中実行型 | **可能** |
| §4.2 単発実行型 | **実質不可**。1 回実行にはグローバル `HotkeyHandler` が要り、**それを使うと他の全項目の既定処理が止まる** |
| §4.3 パッチ型 | 可能だが、`SetEffectMenuJudgment` を束縛の有無へ**毎フレーム手で同期**する必要がある |

**3 型を同じメニューに混在させられない。** 仕様書の項目は 49 件あり、このままでは破綻する。

> 2026-09-17 時点: §4.3 の駆動規則はエンジンの既定になり、`SetHotkeyHandler` / `SetEffectMenuJudgment` は廃止した（§6）。
> §4.2 単発実行型をチェック項目で表す手段（項目ごとの発火関数）はまだ無い。以下の案の採否は未定のまま。

### 7.2 提案

変えるのは**登録の層だけ**。`ToggleEffect` / `TogglePatch` / `PatchList*` / `FlushMemory` はそのまま活かす。

```cpp
enum ItemBehavior : u8
{
    IB_NONE = 0,  // 効果なし（UI 見本）
    IB_PATCH,     // ★既定。複数アドレスを貼る／剥がす（§4.3 パッチ型）
    IB_TICK,      // 有効な間ずっと毎フレーム（§4.1 押下中実行型。ホットキーは修飾キー）
    IB_ONESHOT,   // ホットキーのたび 1 回（§4.2 単発実行型）
    IB_APPLY_ONCE,// 適用で 1 回実行し、項目を OFF へ戻す（§5.5 トグル型アクション式）
    IB_CUSTOM,    // 自作の IsActive / SetActive
};

struct ItemSpec
{
    u8                 behavior;
    const TogglePatch *patches;    u8 patchCount;    // IB_PATCH
    void             (*OnTick)(int index, u16 held); // IB_TICK
    void             (*OnFire)(int index);           // IB_ONESHOT / IB_APPLY_ONCE
    u16                doneMs;                       // IB_APPLY_ONCE の「実行した」表示時間
    ToggleEffectFuncs  custom;                       // IB_CUSTOM
};

bool GuiMenu::RegisterItem(int index, const ItemSpec *spec);
```

グローバルの `SetHotkeyHandler` / `SetToggleHandlers` は**廃止**し、**項目ごとに閉じる**。

### 7.3 駆動元を規則として組み込む

`SetEffectMenuJudgment` を手で同期する代わりに、§4.3 の規則をそのまま engine の既定にする。

> **ホットキーが束縛されていなければメニューが駆動し、束縛されていればホットキーが駆動する。**

- この規則は項目のON/OFFを適用する際に使う。**ホットキー設定だけの追加・変更・解除では効果を変更しない**（§4.3、2026-09-14確定）。
- 実装上も `ApplyItem` が `appliedHotkey` を**先に**更新してから判定へ進むので、
  §4.3 の「ホットキーを設定した状態で項目を ON にしたらフラグは立たない」が**自然に成立する**。

### 7.4 `IB_APPLY_ONCE` とは

**[§5.5](#55-トグル型アクション式確定) のトグル型アクション式を、新しい項目種別を足さずに表すための振る舞い。**

見た目はチェック項目のまま（`[X]` / `ON`）で、**適用したときの意味だけが違う**。

| | 通常のチェック項目 | `IB_APPLY_ONCE` |
|---|---|---|
| 項目 ON を適用 | 効果が ON になり、**その状態が続く** | **アクションを 1 回実行し、項目を OFF へ戻す** |
| 適用後の表示 | `ON` | `OK` を `doneMs` の間出してから `OFF` へ |
| 状態 | 持つ（効果の ON/OFF） | **持たない**。実行したら終わり |

```cpp
// 気絶 — 適用したら 1 回実行して OFF へ戻る
static void FaintFire(int) { /* 気絶状態へ遷移させる */ }
static const GuiMenu::ItemSpec kFaintSpec = {
    GuiMenu::IB_APPLY_ONCE, nullptr, 0, nullptr, FaintFire, 600 /* ms */, {}
};
```

**ホットキーを束縛したときの挙動【確定】**

> メニューでは**ホットキー付きでもON→適用で即実行し、OFFへ戻る**。ホットキーでは**確認ダイアログの承諾後に即実行**する。

- ホットキー設定も適用待ちを経由し、適用後の束縛を使う。設定変更だけの適用では`OnFire`を呼ばない。
- ホットキーを押すと確認ダイアログが開き、承諾したその場で1回実行して`OK`を出す。取消では実行しない。**項目の適用済みON/アームは要求しない**。
- 項目の ON/OFF は変化しない（[§1](#1-用語--2-つの-onoff-を分ける) の規則どおり）。
- ★実装上の注意: 現在のダイアログは**メニューを閉じる時**と**差し戻し**の 2 種類しかない。
  **ホットキー発火時（メニューを閉じている状態）にダイアログを出す経路を新しく作る必要がある。**

メニュー上のONは実行要求の適用待ちを表すもので、ホットキーを受け付けるための持続的なON状態ではない。

### 7.5 振る舞い表

| behavior | 項目 ON を適用（束縛なし） | 項目 ON を適用（束縛あり） | ホットキー | 項目 OFF を適用 |
|---|---|---|---|---|
| **IB_PATCH** | 貼る | **アームのみ** | 貼る／剥がすを反転 | **必ず剥がす** |
| **IB_TICK** | OnTick 開始 | OnTick 開始 | **反転しない**。`held` として OnTick へ渡るだけ | OnTick 停止 |
| **IB_ONESHOT** | 何もしない | アームのみ | **OnFire を 1 回** | — |
| **IB_APPLY_ONCE** | **適用で OnFire を1回＋項目OFF復帰。`doneMs`の間「実行した」と表示** | **同左。ホットキー待機にはしない** | **確認ダイアログ→承諾後に OnFire を1回。ON/アーム不要、項目ON/OFFは変更しない** | 実行せず取り消す |
| **IB_CUSTOM** | `SetActive(true)` | アームのみ | 反転 | `SetActive(false)` |

### 7.6 書き味

```cpp
// 花散らせない — パッチ表を置くだけ。関数を 1 つも書かない
static const GuiMenu::TogglePatch kNoFlowerScatter[] = {
    { 0x00000000, 4, 0xE1A00000, 0x00000000 },   // ★番地は未解析
};
static const GuiMenu::ItemSpec kNoFlowerSpec = {
    GuiMenu::IB_PATCH, kNoFlowerScatter, 1, nullptr, nullptr, 0, {}
};

// アクション解除 — 1 回実行するだけ
static void IdleFire(int) { /* 状態を立ちへ戻す */ }
static const GuiMenu::ItemSpec kIdleSpec = {
    GuiMenu::IB_ONESHOT, nullptr, 0, nullptr, IdleFire, 0, {}
};

// 座標移動 — 押している間だけ
static void MoveTick(int index, u16 held) {
    const u16 hk = GuiMenu::ItemAppliedHotkey(index);
    if (hk == 0 || (held & hk) != hk) return;
    ...
}
static const GuiMenu::ItemSpec kMoveSpec = {
    GuiMenu::IB_TICK, nullptr, 0, MoveTick, nullptr, 0, {}
};
```

**`IB_PATCH` は関数を 1 つも書かずに済む。** 「Vapecord から移植」系の多くがこの形に収まる。

### 7.7 規模

`kMaxItems = 96`、現在 24 項目、仕様書は 49 件。**枠は足りる。**
変更は登録層に閉じるので、`しずえスキップ` と `チャット漢字候補` の既存動作には影響しない。

---

## 8. 現在実装されている項目（2026-09-17）

**`gohan.md` の機能は、まだほとんど実装されていない。**
自前メニューの木は Simulator（`gohan-gui-simulator` `0cabaed`）の `createMenuTree` と同じで、大半は **UI の見た目と操作を確かめるための見本**（ゲームには影響しない）。
項目の木と見本の振る舞いは `Sources/Gui/GuiMenuItems.cpp`。**実機での動作は未確認**（作業用ブランチ `work/simulator-port`）。

### 8.1 実際にゲームへ影響する項目

#### しずえスキップ（チェック項目）

- **ホットキーは割り当てていない。`ToggleEffect` も登録していない。**
- `Framework_ProcOneFrame`（`0x0054C424`）へフックを置き、**毎フレーム**次を見る。
  1. 現在の部屋（`0x0095133A`）が `0x63`（しずえ画面）か。違えば発火済み旗を戻して終わり。
  2. 項目の**適用値**が 0 でないか。
  3. 必要なポインタが **30 フレーム連続**で解決できるか。
- 条件が揃うと **1 回だけ**発火。一括処理（`0x0030C994`。`0x00956231` の boot-once ガード付き）→
  出口（`0x005B464C`）の順に呼び、`村へ移動しました` と通知する。
- 部屋を出ると発火済み旗が戻り、次に `0x63` へ入ればまた発火する。
  **[§5.5](#55-トグル型アクション式確定) と違い、実行後に項目を OFF へ戻さない。**

#### チャット漢字候補（アクション項目）

- 決定すると `ChatKanji::Request()` を 1 回呼ぶ。
- 成功したら下画面に `漢字候補` リストを開く（最初は `変換中です。Bで閉じる`）。
- 失敗したら赤い通知を出す。理由は `REQUEST_BUSY` / `REQUEST_EMPTY` / `REQUEST_NO_CHAT` /
  `REQUEST_BAD_INPUT` / `REQUEST_NO_FONT` / `REQUEST_NO_THREAD` / 版不一致 の 7 種。

### 8.2 振る舞いの見本（ゲームメモリには触らない）

| 項目 | 種別 | 挙動 |
|---|---|---|
| 無敵モード / 壁抜け | チェック | `RegisterToggleEffect` 済み。プラグイン内の bool で §4.3 の規則（束縛なし ON／アーム／OFF で必ず解除／ホットキー反転）が観察できる |
| 連動型数値 / 連動型リスト | 連動型 | `RegisterLinked` 済み。開いた時にプラグイン内の値（1250 / 晴れ）を読み、適用で書く。`S`＋水色・説明欄 `SYNC` |
| トグル型アクション | トグル型アクション | `RegisterExecute` 済み。ON を適用すると実行回数を 1 増やし OFF へ戻る。ホットキーは確認ダイアログ |

### 8.3 UI の見本（ゲームへの影響なし）

| 項目 | 種別 | 実際の動作 |
|---|---|---|
| 名前を変更 / 文字キーボード | アクション | 五十音順キーボードを開く。**入力は使われない**（確定で `名前変更を呼び出しました` と通知） |
| 小型文字キーボード | アクション | 小さいキーボードを開く |
| 上画面リスト / 下画面リスト | アクション | 汎用リストボックスを開く。上画面リストはメニューを閉じても残る |
| **セーブ実行** | アクション | **通知を出すだけ。セーブ関数は呼んでいない** |
| 所持ベル / アイテムID / 移動速度 | 数値 | 左右で増減、A で数値キーボード |
| 通知時間 | スライダー | A で下画面スライダー（左右 1 刻み・上下 5 刻み・タッチ） |
| 天候 / 大量リスト | リスト | その場にリストを展開して選ぶ |
| 歩行速度アップ | チェック | 効果未登録。ON/OFF と通知だけ |
| スクロール項目 ×24 / 追加チート ×12 | チェック | スクロール確認用 |

★**`セーブ実行` はラベルと動作が食い違っている。** 実装時に整理する。

### 8.4 メニュー共通の操作（Simulator と同じ）

- （Simulator 670e44f に追従。2026-09-25）A 決定、B 戻る／閉じる、X 短押しで選択項目を適用、X 600ms 長押しで全項目適用の確認、L 短押しで選択項目を戻す、L 600ms 長押しで全項目戻しの確認、Y でホットキー設定（下画面。全ボタンを離すと確定、タッチで「無効」「取消」）。
- 未適用の変更があるまま閉じると「変更を適用しますか？」。
- 有効（緑）／無効（赤）の通知（題 = 項目名、本文 = `〈項目名〉を有効にしました` / `を無効にしました`）は効果（関数内定義）の ON/OFF が変わったときだけ出す。項目の有効/無効の適用だけでは出さない（§6 (a)）。通知は高さで押し上げて積む（§5.7）。
- START で設定画面、R でお気に入り（§5.7）。X / L の長押しは 2/5 以上・満了前で離すと取り消し（§5.7）。
- メニューの退場アニメーション中はメニュー操作を受け付けない（gohan issue #1）。
- 下画面に操作 UI（入力待ち・キーボード・スライダー・下画面リスト）がある間はゲーム側のタッチを遮断し、UI が消えた後も指が離れるまで遮断を続ける。暗幕は UI の出現量に合わせて一緒にフェードする（issue #2 #3）。
- 文字キーボードの濁点・半濁点キーは「゛」「゜」と表記する（issue #4）。

### 8.5 CTRPF 標準メニュー側の項目

自前メニューとは別に、従来の CTRPF メニュー（`Select + 十字上`）に開発用の 3 項目がある。

| 項目 | 挙動 |
|---|---|
| 右下通知をテスト | 有効化した瞬間に通知を 1 回出す |
| 自前 GUI を組み込む | 自前 GUI の組み込み / 取り外し。**これを ON にしないと自前メニューは使えない** |
| GPU可視メモリ(linear/VRAM)を検証 | 確保経路を調べて SD の `/CTRPF_LinearTest.txt` へ書く |

---

## 9. 未定

**[§4](#4-項目の挙動例確定) と [§5](#5-種別ごとの挙動) の挙動はすべて確定した。** 残るのは次のとおり。

### Simulator で試しながら決めるもの

1. **「実行した」表示の長さ**（[§5.5](#55-トグル型アクション式確定)）。いまは Simulator の値（`OK` を 800ms、行と同じ色）。
2. **連動型で読み取りに失敗した項目の見せ方**（[§5.4](#54-連動型確定)）。いまは「カーソルは置けるが編集できない」（2026-09-17）。

### 設計として決めるもの

3. **[§7](#7-claude案項目ごとの振る舞い記述子) の CLAUDE案の採否。** 利用者が後で判断する。
4. **既定ホットキーの割り当て方法**（`gohan.md` §13 の 6 件）。初期値として焼くか、利用者が束縛するか。
5. **効果の既定値（OFF 値）の決め方**。`TogglePatch` は明示値を要求する。元命令の取得方法と保存先。

6. ~~連動型の「固定」~~ → Simulator 670e44f の形で実装済み（[§5.6](#56-値の固定連動型実装済み実機未確認)）。残るのは固定の禁止の仕組みと、連動型の値の保持の扱い（§5.7）。

### 機能側で決めるもの

7. **`スタイルを変更`専用カスタムメニューの詳細UI**（`gohan.md` §5）。設定・実行をそこで行う方針は確定、具体的な画面/操作は別途。
8. **`セーブ実行` の扱い**（[§8.3](#83-ui-の見本ゲームへの影響なし)）。ラベルと動作が食い違っている。
